# 25 — Assumptions & Open Issues

> Tài liệu #25/00–25 (§14). Sổ đăng ký `[GIẢ ĐỊNH]` + vấn đề mở — cập nhật liên tục. Mọi số ngoài
> Design Basis đều nằm ở đây.

## 1. Sổ giả định (GĐ‑01 … GĐ‑31)
| Mã | Nội dung | Nguồn |
|---|---|---|
| GĐ‑01 | Hệ số tăng tốc AI 1,8–2,5× | doc 00 |
| GĐ‑02 | Person‑month theo tầng (bottom‑up) | doc 00 |
| GĐ‑03 | §10 ≈ 2,5–3 năm → 12 tuần = lát cắt kiến trúc; 3 malfunction W12 (mill trip·tube leak·loss of vacuum) | doc 00 |
| GĐ‑04 | Mã KKS chi tiết (trừ 10LAB10CP001) chờ đối chiếu VGB‑B 106 | doc 01/04/07 |
| GĐ‑05 | Redis current value + pub/sub | doc 02 |
| GĐ‑06 | Video wall dùng chung image hmi | doc 02 |
| GĐ‑07 | Hằng số area=0,8 ví dụ TankLevelModel | doc 03 |
| GĐ‑08 | Enum `requires.engines` (11 engine) | doc 03 |
| GĐ‑09 | SvgPrimitive 4 kind | doc 03 |
| GĐ‑10 | retention_class 3 lớp | doc 04 |
| GĐ‑11 | security_level số nguyên | doc 04 |
| GĐ‑12 | Tag/instance theo template | doc 07 |
| GĐ‑13 | Roll‑up ~3.600 tag | doc 07 |
| GĐ‑14 | Phân bố scan class 15/50/25/10% | doc 07 |
| GĐ‑15 | Alarm/instance theo template | doc 08 |
| GĐ‑16 | Roll‑up ~650 alarm | doc 08 |
| GĐ‑17 | 30 loop; PID params ở doc 10 | doc 09 |
| GĐ‑18 | Thời gian purge & % air NFPA 85 (nguyên tắc) | doc 09 |
| GĐ‑19 | τ/θ các vòng | doc 10 |
| GĐ‑20 | K_swell, hằng số bơm/quạt | doc 10 |
| GĐ‑21 | PID params khởi điểm | doc 10 |
| GĐ‑22 | Số D3 ~52 | doc 12 |
| GĐ‑23 | Hotkey số D2/S | doc 13 |
| GĐ‑24 | ~40 symbol | doc 14 |
| GĐ‑25 | Prisma vs TypeORM | doc 15 |
| GĐ‑26 | device_id chi tiết BoP | doc 16 |
| GĐ‑27 | Endpoint schema sinh từ Zod | doc 17 |
| GĐ‑28 | Sơ đồ zone/conduit theo hạ tầng | doc 18 |
| GĐ‑29 | Danh mục KPI mở rộng | doc 21 |
| GĐ‑30 | Ngưỡng coverage 70% | doc 22 |
| GĐ‑31 | Turborepo vs Nx | doc 23 |
| GĐ‑32 | Hằng số hiệu chỉnh sim Boiler Island (Pha B): DH_EVAP=2758 kJ/kg (coal ~280→steam BMCR 2008), ETA_COMB_MAX=0,94, K_O2_PENALTY=0,01, AF_STOICH=10 kg/kg, AIR_MAX=4000 t/h, K_PRESS=2e‑5, K_SWELL_P=8000, TAU_COAL_ACT=8 s, đường cong SH temp (505+80·tải−95·spray), K_DRAFT=5 Pa/%, MW_PER_TPH=600/2008 (turbine đơn giản hoá) | doc 10 |
| GĐ‑33 | Biên nhiễu đo seeded (LCG, không Math.random): level 0,4 mm · press 0,02 MPa · temp 0,5 °C · O₂ 0,03% · flow 2 t/h · coal 1 t/h · furnace 3 Pa · MW 0,5 | doc 10 §8 |
| GĐ‑34 | Tuning CCS Pha B: gain loop giảm từ doc 10 §9 cho ổn định (pressure kp2/ki0,03 · governor kp3/ki0,3 · fuel kp0,3/ki0,2); FF coordinated (DRAW_TO_FIRING=100/2008 · FIRING_TO_COAL=3 · FIRING_TO_AIR=0,83 · STEAM_TO_FWCV=100/2100); warm-start điểm vận hành ~448 MW | doc 09/10 |
| GĐ‑35 | Setpoint alarm Boiler Island ngoài Design Basis: MSTM-PRESS-HH 19,3 MPa · PRESS-LO 16,0 · SH-TEMP-HH 551 °C · FLUE-O2-LO 1,5% (drum ±250, furnace ±200 neo Design Basis §3.2) | doc 08 |
| GĐ‑36 | Auth lát cắt đơn giản hoá: token dạng chuỗi (TTL 15' + refresh xoay) thay JWT ký thật; 6 user demo mật khẩu 'p'; auto-login Operator; leak/mill-trip (OTS) ánh xạ action 'override'. JWT/SSO/LDAP thật ở sau | doc 05-07/18 |
| GĐ‑37 | Số liệu plugin #2 water-treatment-demo là DEMO tổng hợp: FEED_MAX 200 m³/h · surge tank 60 m³ · SP mức 60% · setpoint alarm (LL10/LO25/HI80/HH90). Mục đích = bài test generic, không phải plant thật | doc 00 §5.4 |
| GĐ‑38 | OTS loss-of-vacuum: mục tiêu 30 kPa (τ 10 s), hệ số công suất = clamp(1−(vac−5,4)/40, 0,5..1) — chân không danh định 5,4 kPa là Design Basis §3.3 | doc 05-05/10 |
| GĐ‑39 | KPI v1: aux power cố định 7% (net = gross×0,93, theo Design Basis §3.1 558/600) — chưa đo house load; KPI tính trên toàn dải historian đã ghi (shift/daily report là v2, doc 21 §3) | doc 21 |
| GĐ‑40 | Maintenance: ngưỡng PM (UNIT1 8000h · MILL 2000h · BFP 4000h); run-state suy từ tag>ngưỡng (MW/coal/fw >10); MTBF = giờ chạy/số hỏng, event hỏng lấy alarm P1 của UNIT1 (v1 đơn giản hoá; CMMS thật = v3) | doc 05-18 |
| GĐ‑41 | Faceplate: interlock blockedReason = null (mô hình interlock first-class để pha sau); lý do chặn LỆNH vẫn hiện đủ qua Control/Security (RBAC/replay/2-step). KKS SH temp 10LBA10CT001 thuộc GĐ-04 (chờ đối chiếu VGB-B 106) | doc 05-15 |
| GĐ‑42 | Seed registry §10 (SeedGenerator + thermalSeedSpec): roll-up thực **3.522 tag / 651 alarm** (≥ 3.000/≥ 600 ✓, khớp bảng doc 07 §4 & ~650 doc 08 §4 — hiệu chỉnh GĐ‑13/16). Phân bố scan_class thực fast 12% / process 56% / slow 14% / diag 18% (lệch GĐ‑14 15/50/25/10 do CEMS+calc dồn diag). KKS breadth = [GIẢ ĐỊNH] có cấu trúc `<unit><sys><nn><mã><seq>` (GĐ‑04). `source='opcua'` = đường thu thập hiện trường bản thật; v1 chỉ mô phỏng lõi Boiler Island (17 tag sim). Registry là DANH MỤC (catalog): Alarm/Tag Engine live chạy subset rationalized, không nạp cả 3.522 tag. id = UUID tất định (cyrb53→splitmix, không Math.random) | doc 07 §5 / doc 08 |
| GĐ‑73 | CAPSTONE cân bằng khối lượng-năng lượng toàn nhà máy (v1.26, doc 10 §6): `PlantBalanceModel` (ISimModel) chạy CUỐI CÙNG — đọc đầu ra MỌI mô hình con → KIỂM CHỨNG CHÉO bảo toàn: nhiệt nhiên liệu = công suất gộp + nhiệt thải bình ngưng + tổn thất lò → `PLANT_ENERGY_CLOSURE_01` **khép ~100 %** (các số ĐỘC LẬP nguồn ⇒ 11 mô hình NHẤT QUÁN; test kiểm cả phát-hiện-bất-nhất khi cố ý sai). KPI toàn nhà máy: `PLANT_ENERGY_IN_01`·`PLANT_BOILER_LOSS_01`·`PLANT_HEAT_REJECT_01`·`PLANT_NET_EFF_01`·`PLANT_UNIT_HR_NET_01`·`PLANT_CO2_INTENSITY_01`·`PLANT_AIR_FLOW_01`. ADDITIVE (0 hồi quy, chỉ tổng hợp). Neo Design Basis: LHV 21.500 · net 558 MW · heat rate đơn vị 9.200 kJ/kWh (η 39 %). **LƯU Ý trung thực:** hiệu suất net mô hình ~33 % < 39 % thiết kế do enthalpy chu trình [GIẢ ĐỊNH] bảo thủ (GĐ-66/68); cân bằng năng lượng VẪN khép ~100 % (nhất quán NỘI BỘ), chênh tuyệt đối với thiết kế là **hạng mục calibrate bằng heat balance thật** — nêu rõ, không giấu. Màn D2-plant-balance (8 ô). Calibrate enthalpy chu trình theo heat balance nhà chế tạo = hạng mục mở M-06 | doc 10 |
| GĐ‑72 | Cung cấp than: bunker/feeder/mill/yard (v1.25, doc 10 §6): `CoalHandlingModel` (ISimModel, CÓ TRẠNG THÁI mức bunker) chạy CẠNH boiler — đọc lưu lượng than tiêu thụ tươi → phía cấp nhiên liệu: `COAL_CONSUMPTION_01` · `COAL_BUNKER_LEVEL_01` (tích phân vào−ra, băng tải hysteresis 60/90 %) · `COAL_CONVEYOR_FEED_01` · `COAL_MILLS_RUNNING_01` (đủ tải ≤ 60 t/h/máy) · `COAL_MILL_LOADING_01`·`COAL_FEEDER_RATE_01` · `COAL_YARD_DAYS_01`. ADDITIVE (0 hồi quy). Neo Design Basis: máy nghiền **6 × 60 t/h (5 chạy + 1 dự phòng)** — ở 283 t/h chạy đúng **5 mill** ✓; đường yard→stacker→băng tải→crusher→bunker→feeder→mill. Sức chứa bunker 2.400 t · băng tải 800 t/h · ngưỡng refill 60/90 % · yard 180.000 t (~26 ngày) = [GIẢ ĐỊNH]. Trạng thái bunker nằm trong snapshot host (OTS). Màn D3-coal-handling (7 ô). Từng mill/feeder riêng + crusher + stacker/reclaimer + coal quality online = pha sau | doc 10 |
| GĐ‑75 | Chiều sâu SCADA cung cấp than — tải TỪNG máy nghiền A–F (v1.27, doc 10 §6): `CoalHandlingModel` phơi thêm `COAL_MILL_A..F_LOAD_01` (6 máy) — `running` máy đầu mang tải ĐỀU (= `COAL_MILL_LOADING_01` trung bình đã tính), phần còn lại DỰ PHÒNG ở 0 %; dùng chính `running`/`millLoading` sẵn có (KHÔNG bịa, không thêm trạng thái, snapshot/restore không đổi). ADDITIVE (0 hồi quy — 7 tag cũ giữ nguyên). Cho phép màn D3-coal-handling vẽ **bản vẽ 6 mill song song** từ bunker gallery → gom về vòi đốt, mỗi mill có tải sống riêng (điểm ~448 MW: 4 mill ~88 % + 2 dự phòng 0 %; đầy tải 5 chạy). Phân bổ tải ĐỀU giữa các mill chạy = [GIẢ ĐỊNH] (thực tế chênh nhẹ theo bias feeder) — thay bằng dữ liệu feeder thật khi có. Sắp mill vào/ra + phân ly than thô/mịn + nhiệt sấy mill = pha sau | doc 10 |
| GĐ‑76 | Chiều sâu SCADA thiết bị SONG SONG (v1.27, doc 10 §6/§7): phơi thêm lưu lượng/tải TỪNG thiết bị trong cụm song song bằng chia đều tổng ĐÃ TÍNH (KHÔNG bịa, không thêm trạng thái): **2 quạt gió FD + 2 quạt khói ID** (`FG_FDA/FDB_FLOW_01` = gió÷2, `FG_IDA/IDB_FLOW_01` = khói÷2) · **2 bơm nước tuần hoàn** (`COND_CWP_A/B_FLOW_01` = 32.000 t/h mỗi bơm) · **4 trường ESP nối tiếp** (`EMI_ESP_IN_DUST_01`+`EMI_ESP_F1..4_DUST_01`, bụi lọt (1−ESP_EFF)^¼ mỗi trường → <30 mg/Nm³ trường cuối = `EMI_DUST_STACK_01`) · **2 thanh cái tự dùng 6,6 kV** (`ELEC_AUX_A/B_MW_01` = tự dùng÷2 qua 2×UAT 50 MVA). ADDITIVE (0 hồi quy — tag tổng giữ nguyên). Cho phép D3-fd-fan/flue-stack/condenser-cw/emissions-cems/electrical vẽ bản vẽ chi tiết cụm song song, mỗi thiết bị 1 giá trị sống. Chia ĐỀU giữa thiết bị giống nhau = [GIẢ ĐỊNH] (thực tế chênh nhẹ do bias/độ mòn) — thay bằng đo từng thiết bị khi có. Trip/standby + chuyển tải khi 1 thiết bị ngừng = pha sau | doc 10 |
| GĐ‑77 | OTS/điều khiển — interlock/permissive first-class + trip tay + mở rộng malfunction (v1.28, doc 09 / W6 DoD): (1) **InterlockEngine** (@idtp/engines) thông dịch luật KHAI BÁO `thermalInterlocks` (7 luật) → chặn lệnh có target kèm LÝ DO (`blockedReason` không còn stub); server chặn `load`/`set-mode`(AUTO)/`seq-live` + faceplate hiện permissive; panel permissive broadcast report-by-exception. (2) **Nút trip tay** MFT/turbine (ghi `BLR_MFT_PB`/`TRB_TRIP_PB`) → cause C&E → hệ quả CHỐT → sim trip THẬT (boiler đã đọc flag trip → cắt than/MW=0); `resetCauseEffect` nhả nút + đánh giá lại rồi mới xoá latch (an toàn). (3) **+6 malfunction** cục bộ theo model: `pa-fan-trip`·`sh-spray-fail`·`feedwater-pump-trip` (boiler) · `cw-pump-trip` (condenser) · `hp-heater-trip` (feedwater) · `ah-fouling` (fluegas) — tổng 11 malfunction + 2 trip tay; `SimulationHost.injectAll` định tuyến tới đúng model (model tự lọc id). ADDITIVE (0 hồi quy). Ngưỡng interlock (áp < 14 MPa · drum ±250 mm · FD < 20% · chân không > 15 kPa) + hệ số malfunction (PA ×0,4 · FW ×0,5 · CW ×0,5 · AH η×0,6/khói ×1,6) = [GIẢ ĐỊNH] — hiệu chỉnh theo bảng logic bảo vệ + thử nghiệm thật khi có. Kịch bản trip đầy đủ `cold-start→…→MFT→coast down` **đã có** (GĐ‑78); actuation C&E vào turbine/điện sâu hơn = pha sau | doc 09 |
| GĐ‑78 | Kịch bản nghiệm thu §10 ĐẦY ĐỦ + SFC thông gió sau trip (v1.29, doc 22): mở rộng `unit-startup-to-coastdown` thành **14 pha** — cold-start (6 SFC: purge·light-off·fw-fill·mill-start·turbine-roll·sync) → ramp 550 → giữ tải → sự cố mill → runback (SFC) → phục hồi mill → **MASTER FUEL TRIP bằng NÚT TAY** (`set BLR_MFT_PB=1` → Cause&Effect chốt → boiler cắt nhiên liệu THẬT, KHÁC bản cũ chỉ đặt tải 0) → coast-down → **thông gió sau trip** (SFC mới `post-trip-purge`, NFPA 85, permissive `BLR_MFT_TRIP ≥ 1`). Thêm SFC thứ 9 `post-trip-purge`; `runScenario` định tuyến malfunction qua `injectAll` (chạm mọi model). Test nghiệm thu: 6 SFC khởi động + post-trip-purge 'ok' (post-trip-purge chỉ 'ok' NẾU MFT thật sự chốt → chứng minh chuỗi end-to-end), MFT_TRIP > 0 + than < 30 + MW < 50% đỉnh ở coast-down. ADDITIVE (0 hồi quy). Số bước settle/ngưỡng pha = [GIẢ ĐỊNH]. Chạy kịch bản LIVE từ HMI (không chặn event-loop) + relight sau purge = pha sau — hiện các mảnh (SFC, MFT, malfunction) đều điều khiển tay được trên HMI | doc 22 |
| GĐ‑79 | Actuation C&E sâu vào turbine/điện (v1.30, doc 09 §4): (1) **TurbineGeneratorModel** đọc `TRB_TRIP`/`TRB_MSV_CLOSE` → van stop đóng → `TRB_STODOLA_FLOW`=0, mất công cơ → rotor **COAST-DOWN** về vùng turning-gear (~200 rpm, τ 30 s), MVAr=0 (trước đây turbine giữ 3000 rpm khi trip — sai). (2) Ma trận `turbine-trip` thêm hệ quả **`GEN_BREAKER_TRIP`** (mở máy cắt máy phát) cho mọi nguyên nhân; **ElectricalModel** đọc → **tách lưới**: `ELEC_GRID_MW_01`=0, MVA/dòng stator=0, tổ máy VẪN nhập tự dùng nền (AUX_BASE 12 MW từ MBA khởi động) → `ELEC_NET_MW_01` **ÂM**; tag mới `ELEC_BREAKER_01` (1=đóng · 0=mở, hiện trên D3-electrical, đỏ khi mở). `resetCauseEffect` xoá `GEN_BREAKER_TRIP` → đóng lại máy cắt. Cascade thật: turbine trip → draw=0 → áp hơi tăng → có thể chạm MFT (ms-press-hh). ADDITIVE (0 hồi quy). COAST_RPM 200 · τ_coast 30 s · tự dùng nền khi tách lưới 12 MW = [GIẢ ĐỊNH]. Rơ-le công suất ngược (32)/mất kích từ (40) + chuyển nguồn tự dùng SUT chi tiết = pha sau | doc 09 |
| GĐ‑80 | Thêm CCS loop LIVE: mức deaerator + mức hotwell (v1.31, doc 09 §1): **7 → 9 vòng điều khiển live** — (1) **deaerator-level**: `FeedwaterTrainModel` thêm mức = tích phân (condensate vào qua LCV − nước cấp ra = hơi); loop giữ 50% qua `FW_DEA_LCV_01`, FF theo hơi (condensate ≈ hơi). (2) **hotwell-level**: `CondenserCWModel` thêm mức = tích phân (ngưng tụ vào ≈ hơi − bơm ngưng CEP ra); loop **reverse** (mức cao → bơm ra nhiều) giữ 50% qua `COND_CEP_LCV_01`, FF theo hơi. Tag mới `FW_DEAERATOR_LEVEL_01` · `COND_HOTWELL_LEVEL_01` (hiện trên D3-feedwater-heatrate/condenser-cw). ADDITIVE (0 hồi quy — toàn tag mới). Test: mức giữ 42–58% ở điểm vận hành + BÁM khi hạ tải 360 MW. Sức chứa deaerator 300 t · hotwell 250 t · LCV_MAX 2.200 t/h · PID kp 0,8/ki 0,05 = [GIẢ ĐỊNH]. Tiến tới 25 loop live còn lại: RH spray (nhiệt tái nhiệt) · BFP recirc · coordinated boiler-turbine master · nhiệt từng tầng SH/rung gối turbine (physics) = pha sau | doc 09 |
| GĐ‑81 | Thêm CCS loop: reheat temp + BFP recirc + coordinated sliding pressure (v1.32, doc 09 §1-2): **9 → 11 loop live**. (1) **reheat-temp**: `ReheatCycleModel` thêm gas-biasing `TRB_RH_BIAS_01` (nâng hot reheat, K 30 °C/100%); loop giữ HRH 541 °C bù droop non tải (không cấp bias → droop như cũ, standalone test không đổi). (2) **bfp-recirc**: loop bảo vệ lưu lượng tối thiểu bơm nước cấp `BLR_BFP_RECIRC_01` (mở khi FW < 350 t/h; đóng ở tải). (3) **coordinated master (sliding pressure)**: `boiler-master-pressure` đổi sang setpoint `BLR_PRESS_SP` — runtime tính TRƯỢT theo tải (≥55% tải giữ 17,5 MPa; dưới đó giảm tuyến tính tới 14 MPa). Ở điểm vận hành (~448 MW, >55%) = 17,5 MPa → **KHÔNG đổi điểm vận hành** (0 hồi quy). ADDITIVE. K_RH_BIAS 30 °C · min-flow 350 t/h · sliding P_min 14/top 55% = [GIẢ ĐỊNH]. Còn (physics chiều sâu B): nhiệt từng tầng SH · rung/nhiệt gối turbine · cell tháp làm mát = increment B-3 | doc 09 |
| GĐ‑82 | Physics chiều sâu B-3: nhiệt tầng SH · gối/rung turbine · profile fill tháp (v1.33, doc 10): (1) **BoilerIslandModel** phơi nhiệt hơi TỪNG TẦNG quá nhiệt `BLR_SH_LTSH_TEMP_01` · `BLR_SH_PLATEN_TEMP_01` (+ final đã có) — nội suy bậc đều từ bão hoà 355 °C tới nhiệt SH cuối. (2) **TurbineGeneratorModel** thêm `TRB_BRG_TEMP_01` (nhiệt gối, quán tính τ 90 s, theo tải) + `TRB_VIB_01` (rung trục: nền 1,8 mm/s ở định mức, ĐỈNH ~11 mm/s khi coast-down đi qua tốc độ tới hạn 1500 rpm — chẩn đoán máy quay). (3) **CoolingTowerModel** thêm profile nhiệt qua fill `CT_CW_HOT_01` (spray header) · `CT_FILL_MID_01` — tháp natural-draft KHÔNG có "cell" như tháp cưỡng bức, nên mô hình profile fill DỌC (nóng→giữa→bể) thay cho cell. Hiện trên D3-superheater/turbine/cooling-tower. ADDITIVE (0 hồi quy — toàn tag mới, phần lớn derived; chỉ nhiệt gối có 1 state, có snapshot). T_SH_SAT 355 °C · gối 45+40·tải · rung base 1,8/spike 9/crit 1500 = [GIẢ ĐỊNH]. Bảng hơi enthalpy thật + phổ rung FFT + psychrometric fill chi tiết = pha sau | doc 10 |
| GĐ‑71 | Tháp làm mát khép vòng CW (v1.24, doc 10 §6): `CoolingTowerModel` (ISimModel) chạy CẠNH …/condenser — đọc nhiệt thải/độ tăng nhiệt CW tươi → khép vòng nước tuần hoàn: `CT_WETBULB_01`·`CT_CW_SUPPLY_01` (= bầu ướt + approach) · `CT_APPROACH_01`·`CT_RANGE_01` · `CT_HEAT_REJECT_01` (= duty bình ngưng) · `CT_EVAP_LOSS_01` (bốc hơi = nhiệt thải/ẩn nhiệt) · `CT_MAKEUP_01` (bổ sung = bốc hơi + blowdown + drift). ADDITIVE (0 hồi quy). Neo Design Basis: tháp natural draft hyperbolic 165 m · CW 64.000 m³/h. Bầu ướt 27 °C (nhiệt đới VN) · approach 4 °C · ẩn nhiệt 2.400 kJ/kg · hệ số bổ sung 1,5 = [GIẢ ĐỊNH]. Bốc hơi ~1.335 t/h ở đầy tải. **KHÉP MÂU THUẪN v1.20:** bầu ướt nhiệt đới 27 °C → CW cấp ~31 °C → chân không THỰC sẽ > 5,4 kPa thiết kế (mốc ôn hoà CW ~19 °C, GĐ-67) — dữ kiện hiệu năng THẬT, không phải lỗi; tháp phản ánh điều kiện địa phương, 5,4 kPa là điểm thiết kế. Màn D3-cooling-tower (7 ô). Fan draft/fill/psychrometric đầy đủ + blowdown chemistry = pha sau | doc 10 |
| GĐ‑70 | Phía điện: máy phát → GSU → lưới + tự dùng → công suất NET (v1.23, doc 10 §7): `ElectricalModel` (ISimModel) chạy CẠNH …/turbine — đọc công suất gộp/phản kháng tươi → `ELEC_AUX_POWER_01` (tự dùng) · `ELEC_NET_MW_01` (net = gộp − tự dùng) · `ELEC_GRID_MW_01` (xuất lưới sau tổn thất GSU) · `ELEC_GEN_MVA_01`·`ELEC_PF_01`·`ELEC_GEN_CURRENT_01` (dòng stator 20 kV) · `ELEC_GSU_LOADING_01`·`ELEC_AUX_LOADING_01`. ADDITIVE (0 hồi quy — không đổi GEN_MW_01/GEN_MVAR_01). Neo Design Basis (Phụ lục A §3.3): gộp/tinh **600/558 MW (tự dùng ~7 %)** · lưới 500 kV/50 Hz · máy phát **667 MVA, 20 kV, cosφ 0,9** · GSU **20/500 kV, 720 MVA** · UAT **2×50 MVA**. Điểm thiết kế (600 MW, cosφ 0,9): net 558 ✓, MVA ~667 ✓, pf 0,9 ✓. Tự dùng nền 12 + biến thiên 30 MW · tổn thất GSU 0,4 % = [GIẢ ĐỊNH]. Lưu ý: sim chạy pf ~0,85 (Q_FACTOR turbine v1.12) → MVA cao hơn ở đầy tải; điểm vận hành ~448 MW tải GSU ~73 %. Màn D3-electrical (8 ô). Kích từ AVR/PSS · bảo vệ ANSI (87/40/46…) · switchyard = pha sau | doc 10 §7 |
| GĐ‑69 | Phát thải CEMS (bụi/SO₂/NOₓ/CO₂ sau ESP+FGD) (v1.22, doc 10 §6): `EmissionsModel` (ISimModel) chạy CẠNH …/fluegas — đọc than/khói/gió thừa tươi → tính tải phát thải từ THÀNH PHẦN THAN (tro/S/C) rồi khử qua ESP/FGD, quy nồng độ mg/Nm³ (CEMS): `EMI_DUST_STACK_01` (< 30 mg/Nm³ đạt mốc DB) · `EMI_SO2_STACK_01` (sau FGD) · `EMI_NOX_STACK_01` · `EMI_CO2_RATE_01` (~640 t/h) · `EMI_FG_VOLUME_01` · `EMI_ESP_EFF_01`·`EMI_FGD_EFF_01`. ADDITIVE (0 hồi quy). Neo Design Basis (Phụ lục A): tro 15 % · S 0,6 % · ESP 2×4 trường bụi ra < 30 mg/Nm³ · sơ đồ khói có FGD. Hàm lượng C 62 % · tỷ lệ tro bay 0,85 · độ khử ESP 0,9977 (hiệu chỉnh đạt < 30) · FGD 0,95 · ρ khói 1,3 · NOₓ nền 320 · thể tích khói riêng = [GIẢ ĐỊNH]. Bảo toàn khối lượng: CO₂ ∝ than (carbon). Màn D3-emissions-cems (7 ô). Số kiểm định/CEMS thật + Hg/CO + FGD gypsum + ESP theo trường = pha sau | doc 10 |
| GĐ‑68 | Đường khói + gió cháy + hiệu suất lò (v1.21, doc 10 §6): `FlueGasAirModel` (ISimModel) chạy CẠNH boiler/…/feedwater — đọc than/O₂/công suất/HR chu trình tươi → khép kín phía KHÓI-GIÓ: `BLR_EFF_01` (hiệu suất lò = nhiệt vào hơi / nhiệt nhiên liệu ~88 %) · `FG_EXCESS_AIR_01`·`FG_LAMBDA_01` (từ O₂) · `FG_FLOW_01` · `FG_AH_GAS_IN_TEMP_01`·`FG_STACK_TEMP_01`·`AH_AIR_OUT_TEMP_01` (air heater) · `FG_DRYGAS_LOSS_01`. ADDITIVE (0 hồi quy, khớp hằng số boiler LHV 21.500 · AF_STOICH 10 · COAL_MAX 300). Neo Design Basis: than bituminous LHV 21.500 kJ/kg · tro 15 % · ẩm 10 % · O₂ 3,2 % · air heater 2×Ljungström · ống khói 210 m. Nhiệt air heater/ống khói (350/130 °C) · cp khói 1,05 · độ hữu hiệu AH 0,84 · T môi trường 30 = [GIẢ ĐỊNH]. **Đối chiếu heat rate:** heat rate ĐƠN VỊ = HR chu trình / hiệu suất lò ≈ 8.940/0,88 ≈ **10.150 kJ/kWh (gross)**; Design Basis **9.200 (net, 39 %)** hàm ý η chu trình cao hơn hằng số enthalpy [GIẢ ĐỊNH] hiện tại (GĐ-66) — chênh ~10 % là hạng mục **HIỆU CHỈNH bằng heat balance thật**, không phải lỗi mô hình (nêu rõ). Màn D3-fluegas-air (8 ô). CEMS/ESP/FGD/tháp làm mát chi tiết = pha sau | doc 10 |
| GĐ‑67 | Bình ngưng + nước tuần hoàn CW (v1.20, doc 10 §6): `CondenserCWModel` (ISimModel) chạy CẠNH boiler/turbine/reheat/feedwater — CÂN BẰNG NĂNG LƯỢNG: nhiệt thải = nhiệt cấp (HR chu trình·gross) − công suất → sinh tag phía CW (`COND_DUTY_01`·`COND_SAT_TEMP_01`·`COND_TTD_01`·`COND_CW_FLOW_01`·`COND_CW_IN/OUT_TEMP_01`·`COND_CW_RISE_01`). ADDITIVE — CHỈ ĐỌC `TRB_COND_VACUUM_01` (boiler sở hữu), KHÔNG ghi đè (0 hồi quy; malfunction loss-of-vacuum vẫn tác động qua boiler). Neo Design Basis (Phụ lục A): CW **64.000 m³/h** · chân không 5,4 kPa → sat ~34 °C · tháp làm mát natural draft. ΔT_cw = Q_thải/(ṁ_cw·cp) ~12 °C; CW ra = sat − TTD; CW vào = ra − ΔT. **Nhiệt thải ~890 MWth > công suất 600** (η<50% → phần lớn nhiệt bị thải — hiện rõ qua cân bằng). TTD 2,8 °C · cp 4,18 · tương quan sat-temp · τ CW = [GIẢ ĐỊNH]. **Lưu ý thật:** 5,4 kPa ⇒ CW vào ~19 °C (ôn hoà); vận hành nhiệt đới (CW vào cao hơn) đẩy back-pressure lên — cảnh báo hiệu năng thật, không phải lỗi mô hình. Màn D3-condenser-cw (7 ô). Fouling/air-ingress động + tháp làm mát chi tiết = pha sau | doc 10 |
| GĐ‑66 | Gia nhiệt HỒI NHIỆT (regenerative) + heat rate chu trình (v1.19, doc 10 §6): `FeedwaterTrainModel` (ISimModel) chạy CẠNH boiler/turbine/reheat — đọc hơi/MW/chân không/nhiệt reheater tươi → sinh tag đoàn gia nhiệt nước cấp (`FW_CONDENSATE_TEMP_01`·`FW_DEAERATOR_TEMP_01`·`FW_ECON_INLET_TEMP_01`·`FW_FLOW_01`·`FW_REGEN_DUTY_01`) + heat rate chu trình (`PLANT_CYCLE_HR_01`). ADDITIVE (0 hồi quy). Neo Design Basis (Phụ lục A): econ inlet **283 °C** · deaerator 0,9 MPa/**178 °C** · condensate ~34 °C (bão hoà 5,4 kPa) · 3 HP + 4 LP heater · đường: condensate→4 LP→deaerator→BFP→3 HP→econ. Nhiệt các mốc trượt theo tải (regFrac = 0,5 + 0,5·loadFrac); regen duty = ṁ·cp·ΔT; heat rate = (ṁ·Δh_boiler + reheat)·3600/gross — nước cấp CÀNG NÓNG → nhiệt cấp càng ÍT → heat rate tốt hơn (lợi ích hồi nhiệt hiện rõ qua chính con số). **Heat rate CHU TRÌNH turbine (~8.900) KHÁC heat rate ĐƠN VỊ 9.200 kJ/kWh** (đơn vị gồm hiệu suất lò ~88% + tự dùng) — nêu rõ, không nhầm lẫn. cp 4,4 kJ/kgK · Δh_boiler 2.145 kJ/kg · đường regFrac · K_vac = [GIẢ ĐỊNH]. Màn D3-feedwater-heatrate (6 ô). Từng heater riêng + drain cascade + bảng hơi enthalpy = pha sau | doc 10 |
| GĐ‑74 | Chiều sâu SCADA đoàn nước cấp — nhiệt đầu ra TỪNG bình gia nhiệt (v1.27, doc 10 §6): `FeedwaterTrainModel` phơi thêm `FW_LPH1..4_TEMP_01` (4 LP heater) + `FW_HPH1..3_TEMP_01` (3 HP heater) — nội suy bậc ĐỀU giữa các nút vật lý ĐÃ CÓ (condensate→deaerator cho LP; deaerator→econ cho HP), CÙNG physics hồi nhiệt GĐ‑66, chỉ tính ở nhiều nút hơn (đơn điệu tăng, tất định, KHÔNG bịa — không thêm trạng thái, snapshot/restore không đổi). ADDITIVE (0 hồi quy — 6 tag cũ giữ nguyên giá trị). Cho phép màn D3-feedwater-heatrate vẽ **bản vẽ bố trí đầy đủ 11 khối** (condensate→4 LP→khử khí→BFP→3 HP→economizer), mỗi bình có nhiệt sống riêng. Chia enthalpy ĐỀU/bình = [GIẢ ĐỊNH] (thực TTD/DCA từng bình khác nhau) — thay bằng heat balance nhà chế tạo khi có. Drain cascade + hơi trích enthalpy từng tầng = pha sau | doc 10 |
| GĐ‑65 | Chu trình TÁI NHIỆT + turbine nhiều tầng (v1.18, doc 10 §6): `ReheatCycleModel` (ISimModel) chạy CẠNH boiler/turbine — đọc hơi chính/áp/nhiệt/MW tươi mỗi bước → sinh THÊM tag đường reheat (`TRB_CRH_PRESS/TEMP_01`, `TRB_HRH_PRESS/TEMP_01`) + tách công suất `TRB_HP/IP/LP_MW_01` + nhiệt lượng `TRB_REHEAT_DUTY_01`. ADDITIVE — các tầng CỘNG lại = `GEN_MW_01` (0 hồi quy, không đổi trục). Neo Design Basis (Phụ lục A): hơi chính 17,5 MPa/541 °C · cold reheat 4,2/330 · hot reheat 3,8/541 · BMCR 2.008 t/h · tandem-compound HP+IP+2×LP. Áp CRH/HRH TRƯỢT theo áp hơi chính (PR_HP = 4,2/17,5); nhiệt CRH bám (hơi − ΔT_HP = 211), HRH điều về ~541 (droop khi non tải); duty = ṁ·Δh_reheat. Phân bổ tầng F_HP/IP/LP = 0,28/0,30/0,42 · Δh_reheat 526 kJ/kg · droop 60 °C · idle 200 °C = [GIẢ ĐỊNH] (suy từ enthalpy drop áp/nhiệt DB; số heat-balance thật lấy khi có). Hiện trên màn D3-turbine-generator (8 ô). Extraction + gia nhiệt hồi nhiệt (3 HP + 4 LP heater) & bảng hơi đầy đủ = pha sau | doc 10 |
| GĐ‑64 | System Diagnostic (v1.17, màn hình hệ thống S — ISA-101): `systemDiagnostics()` READ-ONLY tự soi trạng thái runtime từ NGUỒN THẬT (không bịa): sim (bước·frozen·đồng hồ), tag (số ghi·Good·khác + catalog §10), control loop (tổng·AUTO·MAN·CAS qua `getMode`), historian (điểm qua `size()` + range), alarm (active + P1..P4), journal (tổng·lastSeq). Server lệnh `diag` (read-only, cần login); HMI nút "🩺 Chẩn đoán hệ thống" + modal. Là phương thức APP-LEVEL (sức khoẻ NỀN TẢNG, không phải dữ liệu plant → không cần plugin khai báo, không phá tính generic). Với v1.16/1.17, bộ màn hình hệ thống ISA-101 (S) đã CÓ MẶT ĐỦ: Alarm Summary/History · Trend · Historian-Replay · Event Log · Report · KPI · Maintenance · Engineering (Screen Builder) · User Mgmt (RBAC + audit) · System Diagnostic · AI Advisor. Watchdog/latency/self-test/health-check bên thứ ba = production | ISA-101 |
| GĐ‑63 | Event Log / SOE (v1.16, màn hình hệ thống S — doc 00 §màn hình hệ thống; audit doc 15/18): `EventJournal` GENERIC (ring buffer, seq đơn điệu) ghi lại SỰ KIỆN đã xảy ra — 6 danh mục trung tính (alarm·trip·command·sequence·security·system, KHÔNG tên plugin → giữ tính generic). READ-ONLY với process: chỉ quan sát & ghi, KHÔNG đụng tag/setpoint. Runtime tự bắt: alarm raised/cleared (dedupe theo alarmId), trip C&E chốt (dedupe theo effect), lệnh (đặt tải·ACK·đổi mode·WO·reset C&E·khởi động SFC), hệ thống (tiêm/gỡ malfunction·freeze OTS), chuỗi SFC kết thúc; server đẩy sự kiện bảo mật (login OK/thất bại). `eventLog(query)`/`eventSummary()`; server lệnh `journal` (read-only, cần login); HMI nút "📓 Nhật ký sự kiện" + modal (tổng hợp theo danh mục/mức + bảng giờ·loại·sự kiện·người). Denial lệnh vẫn ở audit RBAC riêng (audit-query, doc 18). Dấu thời gian = đồng hồ sim (không Date.now trong vòng). Lọc nâng cao + xuất + lưu bền = v2 | doc 15/18 |
| GĐ‑62 | Report/KPI engine (v1.15, doc 21): `ReportEngine` GENERIC ráp báo cáo từ section KHAI BÁO (`IReportSection.render(ctx)` → khối table/text/trend), không hardcode nội dung. Đọc aggregate (avg/min/max) qua `IReportContext` (CHỈ ở SDK — plugin không phụ thuộc engine; `range` gắn trong ctx). `thermalReportSections` (3 mục: operating-summary = bảng 6 tag chính avg/min/max · narrative = text từ avg · trend = MW+lưu lượng hơi). Runtime `generateReport(hours)` bơm ctx đọc từ Historian (`query` agg theo range lịch sử); server lệnh `report` (read-only, cần login); HMI nút "📋 Báo cáo ca" + modal render table/text/trend. READ-ONLY tuyệt đối (chỉ đọc lịch sử, không ghi tag). Designer báo cáo kéo-thả + narrative sinh bởi AI + xuất PDF/Excel = v2 | doc 21 |
| GĐ‑61 | Breadth "sống" (v1.14, doc 05-05): `RegistrySimModel` sinh giá trị PLACEHOLDER cho toàn §10 catalog (3.6k tag chưa có physics riêng) → cả nhà máy "thở", mọi màn hình (kể cả 87 màn catalog + màn hình builder) có dữ liệu. Analog ~40% dải + nhiễu LCG có seed (KHÔNG Math.random); bool/int = 0 → không kích alarm breadth (ở biên dải). Chạy stride (mỗi 10 bước) để nhẹ ingest. Opt-in `breadthLive` (mặc định TẮT → test không đụng); server BẬT. Tag sim thật (boiler/turbine) tách biệt, không đụng. Là DỮ LIỆU OTS/demo, KHÔNG physics — [GIẢ ĐỊNH]. Runtime expose `catalogScreens()`; server phục vụ 87 màn qua /screen | doc 05-05 |
| GĐ‑60 | Chốt FEATURE-COMPLETE v1.13 (doc 24): toàn bộ tính năng roadmap v1→v3 đã hiện thực ở mức code — kernel · 12 engine L2 · §10 breadth (3.610 tag/662 alarm/87 màn/29 loop/8 SFC/2 C&E/kịch bản) · trip đổi physics · SFC live · OTS · Historian TimescaleDB + benchmark ≥50k · MQTT Sparkplug · re-sim (+UI banner cam) · AI advisor · Screen Builder L5 (kéo-thả) · Predictive maintenance · Turbine/Generator sim. Thêm màn hình D3-turbine-generator (hiện tag v1.12). **Còn lại = độ chín production** (turbine nhiều tầng/reheat/extraction, sim thật 34 hệ BoP, cắm broker/DB/OPC-UA thật, kiểm định bên thứ ba) — cần hạ tầng/nhân lực/thời gian ngoài phạm vi, ~2–2,5 năm (GĐ-03). build 7/7 · 177 test | doc 24 |
| GĐ‑59 | TurbineGeneratorModel — chiều sâu vật lý (v1.12, doc 10 §6/§7): ISimModel chạy CẠNH boiler (đăng ký sau → đọc hơi/áp/chân không/MW tươi), sinh THÊM tag TRB_STODOLA_FLOW (ellipse Stodola √(pin²−pout²)) · TRB_SPEED_01 (swing quanh 3000 rpm) · GEN_FREQ_01 (speed/60) · GEN_MVAR_01 (0,62·MW ≈ pf 0,85) · GEN_STATOR_TEMP_01 (τ nhiệt). ADDITIVE — không đổi GEN_MW_01 boiler (0 hồi quy). 3000 rpm/50 Hz & vacuum 5,4 kPa neo Design Basis; K_STODOLA/K_IMB/Q_FACTOR/stator = [GIẢ ĐỊNH]. Turbine đầy đủ (nhiều tầng, reheat, extraction) = pha sau | doc 10 |
| GĐ‑58 | Screen Builder UI kéo-thả (v1.11, doc 05-02 L5): HMI modal — chọn tag (picker từ danh sách tag server gửi khi connect), thêm ô, sửa nhãn/đơn vị/kiểu (số/bar), **kéo-thả sắp xếp thứ tự** (HTML5 draggable), số cột → gửi `build-screen` (đã có, action engineer) → mở màn hình vừa tạo. Người không code tạo màn hình hoàn toàn bằng thao tác chuột. Server thêm message `tags` (nguồn picker = recordedTags) | doc 05-02 |
| GĐ‑57 | Predictive Maintenance v1 rule-based (v1.10, doc 05-18/20): `PredictiveMaintenance` READ-ONLY — 3 loại luật: ngưỡng (value≥warn/limit), xu hướng (rate → dự báo giờ tới limit), giờ chạy (tới hạn PM). Trạng thái nhẹ (mẫu tag trước) để tính rate. `thermalPredictiveRules` (5 luật: vacuum trend+threshold, SH temp trend, PM UNIT1/MILL). Runtime `evaluatePredictive()`/`predictiveAdvisories()`; server gộp vào maint broadcast; HMI hiện "🔮" trong maint strip. Ở điểm vận hành không cảnh báo; mất chân không → cảnh báo. Ngưỡng/mốc PM = [GIẢ ĐỊNH] (GĐ-38/40). ML thật = v3 | doc 05-18 |
| GĐ‑56 | Screen Builder L5 (v1.9, doc 05-02): `buildScreen(spec)` — người KHÔNG code chỉ liệt kê tag + nhãn (+alarm/bar tuỳ chọn); engine tự dàn lưới + sinh binding → ScreenDef hợp lệ, kernel render. Server lệnh `build-screen` (action engineer) dựng + đăng ký vào screensById/registry → phục vụ ngay qua /screen/{id}; HMI nút "🧩 Tạo màn hình demo" → mở màn hình vừa dựng. UI kéo-thả đầy đủ = pha sau (bản này bỏ gõ toạ độ/binding tay) | doc 05-02 |
| GĐ‑55 | UI Re-simulation banner cam (v1.8, doc 05-05 §4): server lệnh `resim` (read-only) chạy `reSimulate` nhánh what-if (malfunction hoặc override tải) → trả quỹ đạo; HMI 2 nút "What-if", overlay **banner cam** + biểu đồ canvas MW theo bước (500 bước, độc lập sim live). Không chặn khi replay (read-only, không đụng live) | doc 05-05 |
| GĐ‑54 | AI Advisor v1 rule-based (v1.7, doc 20): `AiAdvisor` READ-ONLY tuyệt đối (chỉ đọc alarm/tag/tri thức; KHÔNG ghi tag/setpoint/ACK). Giải thích alarm = hậu quả + nguyên nhân (theo điều kiện+ngưỡng) + khắc phục + LEO THANG (nếu tag là cause của ma trận C&E) + TRÍCH DẪN nguồn (SOP/narrative/cause-effect + tag + thời gian). Chống bịa: chỉ trích cái có thật. Tri thức plugin (`thermalKnowledge`: 4 doc SOP/C&E/narrative). Server lệnh `advise` (read-only, không chặn replay); HMI nút "💡 Giải thích" + panel. RAG/LLM thật = v2 | doc 20 |
| GĐ‑53 | Re-simulation what-if (v1.6, doc 05-05 §4): `reSimulate()` chạy NHÁNH mô phỏng ĐỘC LẬP — SimulationHost + ControlLoopEngine + kho tag riêng, khôi phục từ snapshot live, chạy kịch bản khác (override/malfunction) → quỹ đạo tag. Sim/tag LIVE KHÔNG bị đụng (kho tag riêng; chỉ đọc snapshot live). Loop nhánh khởi tạo AUTO (transient nhỏ; so sánh tương đối base↔nhánh để loại nhiễu). UI banner cam + biểu đồ quỹ đạo = pha sau (hiện là API + test) | doc 05-05 |
| GĐ‑52 | Driver MQTT Sparkplug B (v1.5, doc 16): `SparkplugDriver` (IProtocolDriver) vai trò HOST — nhận NBIRTH/NDATA, xử lý ĐÚNG alias (BIRTH lập name↔alias, DATA chỉ gửi alias), lọc theo interest → `ctx.publish` vào kernel; `write`→NCMD. `MqttTransport` + `SpCodec` INJECT → lõi không phụ thuộc mqtt.js/protobuf, kiểm được không cần broker. Serialize mặc định JSON; deploy dùng protobuf Eclipse Tahu. metricMap (name↔tag id) do cấu hình cấp | doc 16 |
| GĐ‑51 | Historian TimescaleDB + benchmark (v1.4, doc 19/24): `TimescaleHistorian` cùng chữ ký write/query — đạt mốc ghi **≥ 50.000 điểm/s** bằng write() ĐỆM đồng bộ + flush() BATCH (1 INSERT nhiều dòng). SQL qua `SqlExecutor` INJECT (deploy: node-postgres; test: executor giả) → lõi `@idtp/engines` KHÔNG phụ thuộc `pg`, kiểm được không cần DB. Benchmark loadgen (100k điểm tất định): Memory ~772k điểm/s, Timescale-đệm ~4,37M điểm/s (≫ 50k, biên rộng; throughput xuống DB thật đo tại triển khai). `docker-compose.yml` (Timescale 2.17-pg16) + `docs/benchmark.md`. Ngưỡng bucket/chu kỳ flush = [GIẢ ĐỊNH] | doc 19/24 |
| GĐ‑50 | OTS control UI (v1.3): server + HMI điều khiển OTS trực quan — nút chạy SFC "live" + panel Cause&Effect (trạng thái trip + Reset). RBAC: `seq-live-start` & `ce-reset` = action `engineer` (Engineer+, single-step) — cùng lớp OTS freeze; broadcast `seq-live`/`ce` report-by-exception, gửi `seq-list`/`ce` khi kết nối. (Chạy SFC là chức năng OTS/huấn luyện → gate Engineer, không thêm PermissionAction mới) | doc 05-07 |
| GĐ‑49 | SFC nối vào sim (v1.2, doc 09/10): sim đọc tag lệnh chuỗi → `BLR_FW_FILL_CMD` bơm điền lò (FILL_TPH=300 t/h [GIẢ ĐỊNH]) nâng mức; `BLR_MILL_A_STOP_CMD` bớt 1 mill công suất (mill-a-start xoá cờ). Runtime thêm đường chạy SFC "live" (tick theo nhịp sim, đồng hồ THẬT) để chuỗi tác động physics qua thời gian — khác runSequenceToCompletion (đồng hồ ảo, không bước sim). Ở điểm vận hành các tác động này nằm trong dải bù của CCS (đúng thực tế); kiểm chứng vật lý ở mức sim (không CCS) + kiểm live execution ở runtime. Cascade turbine-trip→áp tăng→MFT: driver (áp tăng khi draw=0) kiểm ở sim; C&E hoàn tất khi áp>19,3 | doc 09/10 |
| GĐ‑48 | Trip đổi PHYSICS (v1.1, doc 10 §9): BoilerIslandModel đọc flag trip do C&E chốt → **MFT** (BLR_MFT_TRIP/MILLS_TRIP/FUEL_VALVES_CLOSE/PA_FANS_TRIP) cắt than về 0 → hơi/áp sập; **turbine trip** (TRB_TRIP/MSV_CLOSE) → đóng draw, MW=0, áp tăng. MW = min(lệnh, hơi sinh)×hệ số chân không → MFT làm MW tụt theo hơi. resetCauseEffect xoá luôn flag → phục hồi (OTS: trip→reset→restart). Vận hành thường không đổi (flag mặc định 0). Hệ quả: C&E tự tác động trên CCS live — leak mạnh (≥ công suất bù ~600 t/h) đẩy drum tới −250 → MFT (test alarm bắt LOW ngay lúc nổi thay vì chờ trạng thái cuối) | doc 10 §9 |
| GĐ‑47 | CauseEffectEngine + thermalCauseEffect (doc 09 §4, NFPA 85): **2 ma trận** boiler-MFT (6 nguyên nhân × 4 hệ quả) + turbine-trip (3×2) — §10 C&E ✓. Engine generic latch trip, reset chỉ khi hết nguyên nhân. Đánh giá mỗi bước trên CCS thật; test kiểm inject loss-of-vacuum → turbine trip chốt. Hệ quả ghi tag flag `*_TRIP` (actuation nối vào sim = sâu hơn, để sau); ngưỡng ngoài Design Basis (vacuum>20 kPa, ms>19,3) = [GIẢ ĐỊNH] | doc 09 §4 |
| GĐ‑46 | ScenarioRunner + thermalScenarios (doc 22, nghiệm thu §10): kịch bản **cold-start→light-off→fill→mill start→turbine roll→sync→ramp 550→mill trip→runback→MFT/coast-down** (10 pha) chạy end-to-end trên CCS thật — test kiểm quỹ đạo MW (ramp lên, coast-down xuống). Engine generic ráp SFC + malfunction + tải qua ScenarioHost. Pha khởi động chạy SFC mang tính ĐIỀU PHỐI (sim warm-start ở điểm vận hành; physics cold-start đầy đủ từ nguội = pha sim sâu hơn, để sau). MFT mô phỏng bằng cắt tải (=0); số bước settle = [GIẢ ĐỊNH] | doc 22 |
| GĐ‑45 | SequenceEngine + thermalSequences (doc 09, ISA-88/NFPA 85): **8 chuỗi SFC khai báo** (mill start/stop · boiler purge · light-off · feedwater fill · turbine roll · gen sync · unit runback) — §10 "8 sequence" ✓. Engine generic thông dịch SequenceDef (permissive-gate · actions ghi có audit · transition tag + holdMs · timeout→failed), không hardcode logic bước. Thời gian giữ/ngưỡng bước = [GIẢ ĐỊNH] (mô phỏng, tinh chỉnh khi vận hành); tag lệnh `*_CMD` là điểm breadth. runSequenceToCompletion chạy đồng hồ ảo (không đụng đồng hồ/CCS sim) | doc 09 |
| GĐ‑44 | LoopGenerator (doc 09): định nghĩa GENERIC "loop = instance có cả `_PV` + `_OP`" → sinh **29 ControlLoopDef** breadth (≥ 25 §10 ✓), chưa gồm 7 loop CCS live đã tinh chỉnh của Boiler Island. Là DANH MỤC: mode khởi tạo MAN, tuning mặc định kp1/ki0,1/kd0 + SP=trung điểm dải PV ([GIẢ ĐỊNH], tinh chỉnh khi đưa vào vận hành). Thêm 4 nhóm control_loop BoP (FGD·than·khí nén·HVAC) → registry lên **3.610 tag / 662 alarm / 87 màn hình** | doc 09 |
| GĐ‑43 | ScreenGenerator (doc 12): sinh **87 màn hình** khai báo từ registry (47 D3/nhóm thiết bị · 18 D2/cell tổng quan · 18 D4/cell chẩn đoán), đạt §10 ≥ 70. Là DANH MỤC: không ép vào nav live (tránh ô rỗng vì đa số tag breadth chưa có dữ liệu sim); nav live vẫn dùng 3 màn hình boiler thủ công. Ô đại diện = float đầu (D3/D2) hoặc tag có alarm (D4); trần 12 ô/màn hình | doc 12 |

## 2. Vấn đề mở
> ✔ M‑05 (bài test generic): thêm `water-treatment-demo` chỉ chạm `plugins/water-treatment-demo/*` + `pnpm-lock.yaml` — **0 dòng sửa `packages/*` & `apps/*`** (kiểm bằng `git status`). Luận điểm "mọi nhà máy chỉ là plugin" đứng vững.
> ✔ Doc 06 (process-analysis-thermal): **45 hệ thống × 13 mục** + 4 mục chuyên sâu (MFT/turbine-trip C&E · purge NFPA 85 · generator protection ANSI) — đạt §10 ≥ 40 hệ. Tag/alarm/màn hình/model ánh xạ hiện thực đã dựng; hệ BoP "(breadth)" có tag danh mục sống, physics riêng = pha sau.
> ✔ Doc 05 (engine-specs): **21 engine L1/L2 × 10 mục §8** (Mục đích/ranh giới · Interface TS · Mô hình dữ liệu · Luồng Mermaid · Cấu hình · Phi chức năng · Chế độ lỗi · Plugin mở rộng · Kiểm thử · Quyết định thiết kế). Chữ ký interface ánh xạ mã đã hiện thực; Trend/Animation (L4 client) không lập chương riêng. → **Bộ tài liệu 00–25 nay đã đủ file** (nợ doc 05/06 đã trả).
> ✔ CI (doc 23): `.github/workflows/ci.yml` — build + test toàn workspace trên mỗi push/PR (matrix Node 20/22, pnpm 9.7.0, frozen lockfile). Cổng kiểm tra hồi quy tự động (232 test).
> ✔ Seed §10 (doc 07 §5): `SeedGenerator` (engine generic) + `thermalSeedSpec` (dữ liệu plugin) → **3.522 tag / 651 alarm**, đạt ngưỡng nghiệm thu §10 (≥ 3.000 / ≥ 600). Bề rộng = thêm DỮ LIỆU khai báo, engine/kernel không đổi (GĐ‑42).
| # | Vấn đề | Cần |
|---|---|---|
| M‑01 | Hiệu chỉnh GĐ‑02 (person‑month) bằng velocity thực Pha A | đo khi code |
| M‑02 | Đối chiếu toàn bộ KKS với VGB‑B 106 | tài liệu chuẩn |
| M‑03 | Xác nhận có FGD hay không (doc 06‑39) | chủ đầu tư |
| M‑04 | Tuning PID + τ/θ trên sim thực | pha code |
| M‑05 | Mốc chuyển lát cắt → v1‑complete | lập lịch doc 24 |
| M‑06 | **Hiệu suất net mô hình ~33 % vs thiết kế 39 %** (heat rate ~10.900 vs 9.200). **QUYẾT ĐỊNH v1 (C1): GIỮ 33 % TRUNG THỰC** — không ép số cho đẹp; với digital twin, số suy ra nhất quán nội bộ > số cosmetic. Đã THỬ hiệu chỉnh 1 hằng số `DH_EVAP` 2758→2340 (GĐ‑32): **phá vỡ điểm vận hành CCS** (áp sập ~8 MPa · cân bằng NL vỡ · ~10 test sub‑model đỏ · trip giả) → **hoàn tác**. Kết luận: 33 % gắn chặt operating point + seed/gain 7 vòng (GĐ‑32) + ngưỡng mọi sub‑model ⇒ chỉ sửa ĐÚNG bằng **tái dẫn xuất TOÀN BỘ điểm vận hành**, KHÔNG phải tweak hằng số | heat balance nhà chế tạo (bắt buộc) + re‑tune seed/gain + cập nhật ~10 test |

## 3. Chống bịa
Mọi số không có trong Design Basis đã gắn `[GIẢ ĐỊNH]` và đăng ký ở đây. Không trích số điều khoản
tiêu chuẩn khi không chắc — mô tả nguyên tắc.
