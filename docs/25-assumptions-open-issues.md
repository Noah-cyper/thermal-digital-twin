# 25 — Assumptions & Open Issues

> Tài liệu #25/00–25 (§14). Sổ đăng ký `[GIẢ ĐỊNH]` + vấn đề mở — cập nhật liên tục. Mọi số ngoài
> Design Basis đều nằm ở đây.

## 1. Sổ giả định (GĐ‑01 … GĐ‑31)
| Mã | Nội dung | Nguồn |
|---|---|---|
| GĐ‑01 | Hệ số tăng tốc AI 1,8–2,5× | doc 00 |
| GĐ‑02 | Person‑month theo tầng (bottom‑up) | doc 00 |
| GĐ‑03 | §10 ≈ 2,5–3 năm → 12 tuần = lát cắt kiến trúc; 3 malfunction W12 (mill trip·tube leak·loss of vacuum) | doc 00 |
| GĐ‑04 | Mã KKS chi tiết (trừ 10LAB10CP001) chờ đối chiếu VGB‑B 106 | doc 01/04/07 |
| GĐ‑05 | Redis current value + pub/sub | doc 02 |
| GĐ‑06 | Video wall dùng chung image hmi | doc 02 |
| GĐ‑07 | Hằng số area=0,8 ví dụ TankLevelModel | doc 03 |
| GĐ‑08 | Enum `requires.engines` (11 engine) | doc 03 |
| GĐ‑09 | SvgPrimitive 4 kind | doc 03 |
| GĐ‑10 | retention_class 3 lớp | doc 04 |
| GĐ‑11 | security_level số nguyên | doc 04 |
| GĐ‑12 | Tag/instance theo template | doc 07 |
| GĐ‑13 | Roll‑up ~3.600 tag | doc 07 |
| GĐ‑14 | Phân bố scan class 15/50/25/10% | doc 07 |
| GĐ‑15 | Alarm/instance theo template | doc 08 |
| GĐ‑16 | Roll‑up ~650 alarm | doc 08 |
| GĐ‑17 | 30 loop; PID params ở doc 10 | doc 09 |
| GĐ‑18 | Thời gian purge & % air NFPA 85 (nguyên tắc) | doc 09 |
| GĐ‑19 | τ/θ các vòng | doc 10 |
| GĐ‑20 | K_swell, hằng số bơm/quạt | doc 10 |
| GĐ‑21 | PID params khởi điểm | doc 10 |
| GĐ‑22 | Số D3 ~52 | doc 12 |
| GĐ‑23 | Hotkey số D2/S | doc 13 |
| GĐ‑24 | ~40 symbol | doc 14 |
| GĐ‑25 | Prisma vs TypeORM | doc 15 |
| GĐ‑26 | device_id chi tiết BoP | doc 16 |
| GĐ‑27 | Endpoint schema sinh từ Zod | doc 17 |
| GĐ‑28 | Sơ đồ zone/conduit theo hạ tầng | doc 18 |
| GĐ‑29 | Danh mục KPI mở rộng | doc 21 |
| GĐ‑30 | Ngưỡng coverage 70% | doc 22 |
| GĐ‑31 | Turborepo vs Nx | doc 23 |
| GĐ‑32 | Hằng số hiệu chỉnh sim Boiler Island (Pha B): DH_EVAP=2758 kJ/kg (coal ~280→steam BMCR 2008), ETA_COMB_MAX=0,94, K_O2_PENALTY=0,01, AF_STOICH=10 kg/kg, AIR_MAX=4000 t/h, K_PRESS=2e‑5, K_SWELL_P=8000, TAU_COAL_ACT=8 s, đường cong SH temp (505+80·tải−95·spray), K_DRAFT=5 Pa/%, MW_PER_TPH=600/2008 (turbine đơn giản hoá) | doc 10 |
| GĐ‑33 | Biên nhiễu đo seeded (LCG, không Math.random): level 0,4 mm · press 0,02 MPa · temp 0,5 °C · O₂ 0,03% · flow 2 t/h · coal 1 t/h · furnace 3 Pa · MW 0,5 | doc 10 §8 |
| GĐ‑34 | Tuning CCS Pha B: gain loop giảm từ doc 10 §9 cho ổn định (pressure kp2/ki0,03 · governor kp3/ki0,3 · fuel kp0,3/ki0,2); FF coordinated (DRAW_TO_FIRING=100/2008 · FIRING_TO_COAL=3 · FIRING_TO_AIR=0,83 · STEAM_TO_FWCV=100/2100); warm-start điểm vận hành ~448 MW | doc 09/10 |
| GĐ‑35 | Setpoint alarm Boiler Island ngoài Design Basis: MSTM-PRESS-HH 19,3 MPa · PRESS-LO 16,0 · SH-TEMP-HH 551 °C · FLUE-O2-LO 1,5% (drum ±250, furnace ±200 neo Design Basis §3.2) | doc 08 |
| GĐ‑36 | Auth lát cắt đơn giản hoá: token dạng chuỗi (TTL 15' + refresh xoay) thay JWT ký thật; 6 user demo mật khẩu 'p'; auto-login Operator; leak/mill-trip (OTS) ánh xạ action 'override'. JWT/SSO/LDAP thật ở sau | doc 05-07/18 |
| GĐ‑37 | Số liệu plugin #2 water-treatment-demo là DEMO tổng hợp: FEED_MAX 200 m³/h · surge tank 60 m³ · SP mức 60% · setpoint alarm (LL10/LO25/HI80/HH90). Mục đích = bài test generic, không phải plant thật | doc 00 §5.4 |
| GĐ‑38 | OTS loss-of-vacuum: mục tiêu 30 kPa (τ 10 s), hệ số công suất = clamp(1−(vac−5,4)/40, 0,5..1) — chân không danh định 5,4 kPa là Design Basis §3.3 | doc 05-05/10 |
| GĐ‑39 | KPI v1: aux power cố định 7% (net = gross×0,93, theo Design Basis §3.1 558/600) — chưa đo house load; KPI tính trên toàn dải historian đã ghi (shift/daily report là v2, doc 21 §3) | doc 21 |
| GĐ‑40 | Maintenance: ngưỡng PM (UNIT1 8000h · MILL 2000h · BFP 4000h); run-state suy từ tag>ngưỡng (MW/coal/fw >10); MTBF = giờ chạy/số hỏng, event hỏng lấy alarm P1 của UNIT1 (v1 đơn giản hoá; CMMS thật = v3) | doc 05-18 |
| GĐ‑41 | Faceplate: interlock blockedReason = null (mô hình interlock first-class để pha sau); lý do chặn LỆNH vẫn hiện đủ qua Control/Security (RBAC/replay/2-step). KKS SH temp 10LBA10CT001 thuộc GĐ-04 (chờ đối chiếu VGB-B 106) | doc 05-15 |
| GĐ‑42 | Seed registry §10 (SeedGenerator + thermalSeedSpec): roll-up thực **3.522 tag / 651 alarm** (≥ 3.000/≥ 600 ✓, khớp bảng doc 07 §4 & ~650 doc 08 §4 — hiệu chỉnh GĐ‑13/16). Phân bố scan_class thực fast 12% / process 56% / slow 14% / diag 18% (lệch GĐ‑14 15/50/25/10 do CEMS+calc dồn diag). KKS breadth = [GIẢ ĐỊNH] có cấu trúc `<unit><sys><nn><mã><seq>` (GĐ‑04). `source='opcua'` = đường thu thập hiện trường bản thật; v1 chỉ mô phỏng lõi Boiler Island (17 tag sim). Registry là DANH MỤC (catalog): Alarm/Tag Engine live chạy subset rationalized, không nạp cả 3.522 tag. id = UUID tất định (cyrb53→splitmix, không Math.random) | doc 07 §5 / doc 08 |
| GĐ‑73 | CAPSTONE cân bằng khối lượng-năng lượng toàn nhà máy (v1.26, doc 10 §6): `PlantBalanceModel` (ISimModel) chạy CUỐI CÙNG — đọc đầu ra MỌI mô hình con → KIỂM CHỨNG CHÉO bảo toàn: nhiệt nhiên liệu = công suất gộp + nhiệt thải bình ngưng + tổn thất lò → `PLANT_ENERGY_CLOSURE_01` **khép ~100 %** (các số ĐỘC LẬP nguồn ⇒ 11 mô hình NHẤT QUÁN; test kiểm cả phát-hiện-bất-nhất khi cố ý sai). KPI toàn nhà máy: `PLANT_ENERGY_IN_01`·`PLANT_BOILER_LOSS_01`·`PLANT_HEAT_REJECT_01`·`PLANT_NET_EFF_01`·`PLANT_UNIT_HR_NET_01`·`PLANT_CO2_INTENSITY_01`·`PLANT_AIR_FLOW_01`. ADDITIVE (0 hồi quy, chỉ tổng hợp). Neo Design Basis: LHV 21.500 · net 558 MW · heat rate đơn vị 9.200 kJ/kWh (η 39 %). **LƯU Ý trung thực:** hiệu suất net mô hình ~33 % < 39 % thiết kế do enthalpy chu trình [GIẢ ĐỊNH] bảo thủ (GĐ-66/68); cân bằng năng lượng VẪN khép ~100 % (nhất quán NỘI BỘ), chênh tuyệt đối với thiết kế là **hạng mục calibrate bằng heat balance thật** — nêu rõ, không giấu. Màn D2-plant-balance (8 ô). Calibrate enthalpy chu trình theo heat balance nhà chế tạo = hạng mục mở M-06 | doc 10 |
| GĐ‑72 | Cung cấp than: bunker/feeder/mill/yard (v1.25, doc 10 §6): `CoalHandlingModel` (ISimModel, CÓ TRẠNG THÁI mức bunker) chạy CẠNH boiler — đọc lưu lượng than tiêu thụ tươi → phía cấp nhiên liệu: `COAL_CONSUMPTION_01` · `COAL_BUNKER_LEVEL_01` (tích phân vào−ra, băng tải hysteresis 60/90 %) · `COAL_CONVEYOR_FEED_01` · `COAL_MILLS_RUNNING_01` (đủ tải ≤ 60 t/h/máy) · `COAL_MILL_LOADING_01`·`COAL_FEEDER_RATE_01` · `COAL_YARD_DAYS_01`. ADDITIVE (0 hồi quy). Neo Design Basis: máy nghiền **6 × 60 t/h (5 chạy + 1 dự phòng)** — ở 283 t/h chạy đúng **5 mill** ✓; đường yard→stacker→băng tải→crusher→bunker→feeder→mill. Sức chứa bunker 2.400 t · băng tải 800 t/h · ngưỡng refill 60/90 % · yard 180.000 t (~26 ngày) = [GIẢ ĐỊNH]. Trạng thái bunker nằm trong snapshot host (OTS). Màn D3-coal-handling (7 ô). Từng mill/feeder riêng + crusher + stacker/reclaimer + coal quality online = pha sau | doc 10 |
| GĐ‑75 | Chiều sâu SCADA cung cấp than — tải TỪNG máy nghiền A–F (v1.27, doc 10 §6): `CoalHandlingModel` phơi thêm `COAL_MILL_A..F_LOAD_01` (6 máy) — `running` máy đầu mang tải ĐỀU (= `COAL_MILL_LOADING_01` trung bình đã tính), phần còn lại DỰ PHÒNG ở 0 %; dùng chính `running`/`millLoading` sẵn có (KHÔNG bịa, không thêm trạng thái, snapshot/restore không đổi). ADDITIVE (0 hồi quy — 7 tag cũ giữ nguyên). Cho phép màn D3-coal-handling vẽ **bản vẽ 6 mill song song** từ bunker gallery → gom về vòi đốt, mỗi mill có tải sống riêng (điểm ~448 MW: 4 mill ~88 % + 2 dự phòng 0 %; đầy tải 5 chạy). Phân bổ tải ĐỀU giữa các mill chạy = [GIẢ ĐỊNH] (thực tế chênh nhẹ theo bias feeder) — thay bằng dữ liệu feeder thật khi có. Sắp mill vào/ra + phân ly than thô/mịn + nhiệt sấy mill = pha sau | doc 10 |
| GĐ‑76 | Chiều sâu SCADA thiết bị SONG SONG (v1.27, doc 10 §6/§7): phơi thêm lưu lượng/tải TỪNG thiết bị trong cụm song song bằng chia đều tổng ĐÃ TÍNH (KHÔNG bịa, không thêm trạng thái): **2 quạt gió FD + 2 quạt khói ID** (`FG_FDA/FDB_FLOW_01` = gió÷2, `FG_IDA/IDB_FLOW_01` = khói÷2) · **2 bơm nước tuần hoàn** (`COND_CWP_A/B_FLOW_01` = 32.000 t/h mỗi bơm) · **4 trường ESP nối tiếp** (`EMI_ESP_IN_DUST_01`+`EMI_ESP_F1..4_DUST_01`, bụi lọt (1−ESP_EFF)^¼ mỗi trường → <30 mg/Nm³ trường cuối = `EMI_DUST_STACK_01`) · **2 thanh cái tự dùng 6,6 kV** (`ELEC_AUX_A/B_MW_01` = tự dùng÷2 qua 2×UAT 50 MVA). ADDITIVE (0 hồi quy — tag tổng giữ nguyên). Cho phép D3-fd-fan/flue-stack/condenser-cw/emissions-cems/electrical vẽ bản vẽ chi tiết cụm song song, mỗi thiết bị 1 giá trị sống. Chia ĐỀU giữa thiết bị giống nhau = [GIẢ ĐỊNH] (thực tế chênh nhẹ do bias/độ mòn) — thay bằng đo từng thiết bị khi có. Trip/standby + chuyển tải khi 1 thiết bị ngừng = pha sau | doc 10 |
| GĐ‑77 | OTS/điều khiển — interlock/permissive first-class + trip tay + mở rộng malfunction (v1.28, doc 09 / W6 DoD): (1) **InterlockEngine** (@idtp/engines) thông dịch luật KHAI BÁO `thermalInterlocks` (7 luật) → chặn lệnh có target kèm LÝ DO (`blockedReason` không còn stub); server chặn `load`/`set-mode`(AUTO)/`seq-live` + faceplate hiện permissive; panel permissive broadcast report-by-exception. (2) **Nút trip tay** MFT/turbine (ghi `BLR_MFT_PB`/`TRB_TRIP_PB`) → cause C&E → hệ quả CHỐT → sim trip THẬT (boiler đã đọc flag trip → cắt than/MW=0); `resetCauseEffect` nhả nút + đánh giá lại rồi mới xoá latch (an toàn). (3) **+6 malfunction** cục bộ theo model: `pa-fan-trip`·`sh-spray-fail`·`feedwater-pump-trip` (boiler) · `cw-pump-trip` (condenser) · `hp-heater-trip` (feedwater) · `ah-fouling` (fluegas) — tổng 11 malfunction + 2 trip tay; `SimulationHost.injectAll` định tuyến tới đúng model (model tự lọc id). ADDITIVE (0 hồi quy). Ngưỡng interlock (áp < 14 MPa · drum ±250 mm · FD < 20% · chân không > 15 kPa) + hệ số malfunction (PA ×0,4 · FW ×0,5 · CW ×0,5 · AH η×0,6/khói ×1,6) = [GIẢ ĐỊNH] — hiệu chỉnh theo bảng logic bảo vệ + thử nghiệm thật khi có. Kịch bản trip đầy đủ `cold-start→…→MFT→coast down` **đã có** (GĐ‑78); actuation C&E vào turbine/điện sâu hơn = pha sau | doc 09 |
| GĐ‑78 | Kịch bản nghiệm thu §10 ĐẦY ĐỦ + SFC thông gió sau trip (v1.29, doc 22): mở rộng `unit-startup-to-coastdown` thành **14 pha** — cold-start (6 SFC: purge·light-off·fw-fill·mill-start·turbine-roll·sync) → ramp 550 → giữ tải → sự cố mill → runback (SFC) → phục hồi mill → **MASTER FUEL TRIP bằng NÚT TAY** (`set BLR_MFT_PB=1` → Cause&Effect chốt → boiler cắt nhiên liệu THẬT, KHÁC bản cũ chỉ đặt tải 0) → coast-down → **thông gió sau trip** (SFC mới `post-trip-purge`, NFPA 85, permissive `BLR_MFT_TRIP ≥ 1`). Thêm SFC thứ 9 `post-trip-purge`; `runScenario` định tuyến malfunction qua `injectAll` (chạm mọi model). Test nghiệm thu: 6 SFC khởi động + post-trip-purge 'ok' (post-trip-purge chỉ 'ok' NẾU MFT thật sự chốt → chứng minh chuỗi end-to-end), MFT_TRIP > 0 + than < 30 + MW < 50% đỉnh ở coast-down. ADDITIVE (0 hồi quy). Số bước settle/ngưỡng pha = [GIẢ ĐỊNH]. Chạy kịch bản LIVE từ HMI (không chặn event-loop) + relight sau purge = pha sau — hiện các mảnh (SFC, MFT, malfunction) đều điều khiển tay được trên HMI | doc 22 |
| GĐ‑79 | Actuation C&E sâu vào turbine/điện (v1.30, doc 09 §4): (1) **TurbineGeneratorModel** đọc `TRB_TRIP`/`TRB_MSV_CLOSE` → van stop đóng → `TRB_STODOLA_FLOW`=0, mất công cơ → rotor **COAST-DOWN** về vùng turning-gear (~200 rpm, τ 30 s), MVAr=0 (trước đây turbine giữ 3000 rpm khi trip — sai). (2) Ma trận `turbine-trip` thêm hệ quả **`GEN_BREAKER_TRIP`** (mở máy cắt máy phát) cho mọi nguyên nhân; **ElectricalModel** đọc → **tách lưới**: `ELEC_GRID_MW_01`=0, MVA/dòng stator=0, tổ máy VẪN nhập tự dùng nền (AUX_BASE 12 MW từ MBA khởi động) → `ELEC_NET_MW_01` **ÂM**; tag mới `ELEC_BREAKER_01` (1=đóng · 0=mở, hiện trên D3-electrical, đỏ khi mở). `resetCauseEffect` xoá `GEN_BREAKER_TRIP` → đóng lại máy cắt. Cascade thật: turbine trip → draw=0 → áp hơi tăng → có thể chạm MFT (ms-press-hh). ADDITIVE (0 hồi quy). COAST_RPM 200 · τ_coast 30 s · tự dùng nền khi tách lưới 12 MW = [GIẢ ĐỊNH]. Rơ-le công suất ngược (32)/mất kích từ (40) + chuyển nguồn tự dùng SUT chi tiết = pha sau | doc 09 |
| GĐ‑80 | Thêm CCS loop LIVE: mức deaerator + mức hotwell (v1.31, doc 09 §1): **7 → 9 vòng điều khiển live** — (1) **deaerator-level**: `FeedwaterTrainModel` thêm mức = tích phân (condensate vào qua LCV − nước cấp ra = hơi); loop giữ 50% qua `FW_DEA_LCV_01`, FF theo hơi (condensate ≈ hơi). (2) **hotwell-level**: `CondenserCWModel` thêm mức = tích phân (ngưng tụ vào ≈ hơi − bơm ngưng CEP ra); loop **reverse** (mức cao → bơm ra nhiều) giữ 50% qua `COND_CEP_LCV_01`, FF theo hơi. Tag mới `FW_DEAERATOR_LEVEL_01` · `COND_HOTWELL_LEVEL_01` (hiện trên D3-feedwater-heatrate/condenser-cw). ADDITIVE (0 hồi quy — toàn tag mới). Test: mức giữ 42–58% ở điểm vận hành + BÁM khi hạ tải 360 MW. Sức chứa deaerator 300 t · hotwell 250 t · LCV_MAX 2.200 t/h · PID kp 0,8/ki 0,05 = [GIẢ ĐỊNH]. Tiến tới 25 loop live còn lại: RH spray (nhiệt tái nhiệt) · BFP recirc · coordinated boiler-turbine master · nhiệt từng tầng SH/rung gối turbine (physics) = pha sau | doc 09 |
| GĐ‑81 | Thêm CCS loop: reheat temp + BFP recirc + coordinated sliding pressure (v1.32, doc 09 §1-2): **9 → 11 loop live**. (1) **reheat-temp**: `ReheatCycleModel` thêm gas-biasing `TRB_RH_BIAS_01` (nâng hot reheat, K 30 °C/100%); loop giữ HRH 541 °C bù droop non tải (không cấp bias → droop như cũ, standalone test không đổi). (2) **bfp-recirc**: loop bảo vệ lưu lượng tối thiểu bơm nước cấp `BLR_BFP_RECIRC_01` (mở khi FW < 350 t/h; đóng ở tải). (3) **coordinated master (sliding pressure)**: `boiler-master-pressure` đổi sang setpoint `BLR_PRESS_SP` — runtime tính TRƯỢT theo tải (≥55% tải giữ 17,5 MPa; dưới đó giảm tuyến tính tới 14 MPa). Ở điểm vận hành (~448 MW, >55%) = 17,5 MPa → **KHÔNG đổi điểm vận hành** (0 hồi quy). ADDITIVE. K_RH_BIAS 30 °C · min-flow 350 t/h · sliding P_min 14/top 55% = [GIẢ ĐỊNH]. Còn (physics chiều sâu B): nhiệt từng tầng SH · rung/nhiệt gối turbine · cell tháp làm mát = increment B-3 | doc 09 |
| GĐ‑82 | Physics chiều sâu B-3: nhiệt tầng SH · gối/rung turbine · profile fill tháp (v1.33, doc 10): (1) **BoilerIslandModel** phơi nhiệt hơi TỪNG TẦNG quá nhiệt `BLR_SH_LTSH_TEMP_01` · `BLR_SH_PLATEN_TEMP_01` (+ final đã có) — nội suy bậc đều từ bão hoà 355 °C tới nhiệt SH cuối. (2) **TurbineGeneratorModel** thêm `TRB_BRG_TEMP_01` (nhiệt gối, quán tính τ 90 s, theo tải) + `TRB_VIB_01` (rung trục: nền 1,8 mm/s ở định mức, ĐỈNH ~11 mm/s khi coast-down đi qua tốc độ tới hạn 1500 rpm — chẩn đoán máy quay). (3) **CoolingTowerModel** thêm profile nhiệt qua fill `CT_CW_HOT_01` (spray header) · `CT_FILL_MID_01` — tháp natural-draft KHÔNG có "cell" như tháp cưỡng bức, nên mô hình profile fill DỌC (nóng→giữa→bể) thay cho cell. Hiện trên D3-superheater/turbine/cooling-tower. ADDITIVE (0 hồi quy — toàn tag mới, phần lớn derived; chỉ nhiệt gối có 1 state, có snapshot). T_SH_SAT 355 °C · gối 45+40·tải · rung base 1,8/spike 9/crit 1500 = [GIẢ ĐỊNH]. Bảng hơi enthalpy thật + phổ rung FFT + psychrometric fill chi tiết = pha sau | doc 10 |
| GĐ‑83 | CCS loop C-1 điều khiển môi trường: SCR deNOx (NH₃) + FGD SO₂ (slurry) (v1.34, doc 09 §1): **11 → 13 loop live**. `EmissionsModel` biến hiệu suất khử NOₓ/SO₂ từ HẰNG SỐ → CÓ ĐIỀU KHIỂN. (1) **scr-nox**: NH₃ phun `EMI_NH3_INJ_01` khử NOₓ nền cháy (`EMI_NOX_SCR_IN_01` ~320) → `EMI_NOX_STACK_01` giữ ~150 mg/Nm³ (reverse, feedback outlet-NOx); hiệu suất `EMI_SCR_EFF_01`. (2) **fgd-so2**: slurry đá vôi `EMI_FGD_SLURRY_01` điều khiển `EMI_FGD_EFF_01` giữ SO₂ ~61 mg/Nm³ (reverse). **0 hồi quy nhờ guard demand>0**: khi tag lệnh vắng/=0 (standalone/bypass) → SCR 0 khử (NOₓ = nền), FGD dùng 0,95 thiết kế → cả 2 test emissions cũ (plugin NOₓ∈200–450 & FGD≈95; app NOₓ>100 & SO₂>15) KHÔNG đổi; seed bumpless nh3 62,5%/slurry 55,6% giữ ĐÚNG điểm vận hành hiện tại. Test control-c1 (3): điểm vận hành SCR 320→150 + FGD giữ 61 + lệnh đang cấp · bền khi hạ tải 300 MW · MFT → phát thải + SCR-inlet = 0. Hiện trên D3-emissions-cems (thêm 4 ô: NOₓ vào SCR · hiệu suất SCR · NH₃ · slurry). SCR_MAX 0,85 · FGD 0,90–0,99 theo slurry · SP NOₓ 150/SO₂ 61 = [GIẢ ĐỊNH] — hiệu chỉnh theo CEMS thật + ammonia slip/pH absorber (điều khiển pH FGD thật) khi có. Tiến tới 25 loop: mill outlet temp/PA header · deaerator pegging · gland steam · condensate makeup · HP/LP bypass = pha sau (C-2…) | doc 09 |
| GĐ‑84 | CCS loop C-2 phụ trợ hơi: áp deaerator (pegging) + áp hơi chèn trục (gland) (v1.35, doc 09 §1): **13 → 15 loop live**. Cả hai là **derived thuần** (không thêm state, snapshot không đổi) — thêm tag mới, KHÔNG đụng output cũ ⇒ 0 hồi quy tự nhiên (thuần cộng, khác C-1 phải guard). (1) **deaerator-pressure**: `FeedwaterTrainModel` thêm `FW_DEA_PRESS_01` = áp hơi trích (∝ tải, `P_DEA_EXTRACT_MAX` 1,0 MPa) + van pegging `FW_DEA_PEG_VALVE_01` (`K_PEG` 0,5 MPa toàn hành trình); loop giữ 0,9 MPa (direct: áp thấp→mở). Điểm vận hành hơi trích ~0,75 → pegging mở ~30% bù tới 0,9; hạ tải mở thêm (~80% ở 300 MW). (2) **gland-steam-pressure**: `TurbineGeneratorModel` thêm `TRB_GLAND_PRESS_01` = tự chèn leak-off HP (∝ tải, `GLAND_SELF_SEAL_MAX` 4 kPag) + van cấp `TRB_GLAND_VALVE_01` (`K_GLAND` 6 kPag); loop giữ 5 kPag (direct). Điểm vận hành tự chèn ~3 → van mở ~33% bù tới 5; hạ tải mở thêm. Hiện trên D3-feedwater-heatrate/turbine (mỗi màn +2 ô: áp + van). Test control-c2 (3): điểm vận hành 2 áp = setpoint & van đang cấp · hạ tải 300 MW → van MỞ THÊM giữ setpoint · zero-regression mức deaerator/nhiệt econ không đổi. `P_DEA_EXTRACT_MAX` · `K_PEG` · `GLAND_SELF_SEAL_MAX` · `K_GLAND` · SP 0,9 MPa / 5 kPag = [GIẢ ĐỊNH] — hiệu chỉnh theo dữ liệu trích hơi/leak-off thật khi có. Tiến tới 25: mill outlet temp · PA header · condensate makeup · aux steam · HP/LP bypass = C-3/C-4 | doc 09 |
| GĐ‑85 | CCS loop C-3 hệ gió sơ cấp/mill: nhiệt ra máy nghiền + áp header PA (v1.36, doc 09 §1): **15 → 17 loop live**, cả hai trong `CoalHandlingModel` (hệ mill/gió sơ cấp). (1) **mill-outlet-temp**: `COAL_MILL_OUT_TEMP_01` = trộn gió nóng PA (từ air heater `AH_AIR_OUT_TEMP_01`) + gió lạnh tempering − làm nguội bốc ẩm than (∝ tải mill); van gió nóng `COAL_HOT_AIR_DMPR_01` (direct) giữ 70 °C. **THÊM 1 state `millTemp` (quán tính TAU_MILL 45 s)** vì mục tiêu trộn tức thời có gain lớn (241 °C/toàn hành trình) → loop dao động nếu không có quán tính; snapshot/restore đã thêm. Loop gain hạ (kp 0,4/ki 0,04). (2) **pa-header-pressure**: `COAL_PA_HEADER_PRESS_01` = quạt PA (van hướng `COAL_PA_FAN_VANE_01`) − trở lực mill/vòi đốt (∝ tải); direct giữ 9 kPa (derived thuần, gain nhỏ nên ổn định không cần quán tính). Điểm vận hành: mill 70 °C/van gió nóng ~31%, PA 9 kPa/van quạt ~70%; giữ setpoint qua hạ tải 300 MW. Hiện D3-coal-handling (+4 ô cạnh vòi đốt). `T_TEMPER_AIR` 30 · `T_HOT_PA_FALLBACK` 271 · `K_COAL_DRY_COOL` 40 · `TAU_MILL` 45 s · `K_PA_VANE` 18 · `K_PA_RESIST` 4 · SP 70 °C / 9 kPa = [GIẢ ĐỊNH] — hiệu chỉnh theo dữ liệu mill/PA thật khi có. Test control-c3 (3) + zero-regression số mill/tải mill. Tiến tới 25: condensate makeup · aux steam · HP/LP bypass = C-4 | doc 09 |
| GĐ‑86 | CCS loop C-4 làm mát máy phát: áp khí H₂ + nhiệt nước làm mát stator (v1.37, doc 09 §1): **17 → 19 loop live**, cả hai trong `ElectricalModel` (derived thuần, không state, snapshot không đổi ⇒ 0 hồi quy thuần cộng). (1) **generator-h2-pressure**: `ELEC_H2_PRESS_01` = nền + van cấp H₂ `ELEC_H2_VALVE_01` (direct) giữ 0,4 MPa bù rò seal (độc lập tải — H₂ duy trì cả khi tách lưới). (2) **stator-cooling-temp**: `ELEC_STATOR_CW_TEMP_01` = nước làm mát vào + I²R theo tải − van nước làm mát `ELEC_STATOR_CW_VALVE_01` (reverse: nhiệt cao→mở van) giữ 45 °C. Điểm vận hành: H₂ 0,4 MPa/van ~50%, stator CW 45 °C/van ~53%; hạ tải 300 MW → I²R giảm → van làm mát ĐÓNG bớt (giữ 45), H₂ giữ 0,4. Hiện D3-generator (+4 ô). Neo Design Basis: máy phát H₂-cooled 667 MVA. `P_H2` 0,2+0,4·van · `T_STATOR_CW_AMB` 38 · `K_STATOR_HEAT` 20 · `K_STATOR_COOL` 15 · SP 0,4 MPa / 45 °C = [GIẢ ĐỊNH] — hiệu chỉnh theo dữ liệu làm mát máy phát thật khi có. Test control-c4 (3) + zero-regression net/grid MW. Tới 25 còn 6 vòng: aux steam header · HP/LP bypass · condensate makeup · condenser vacuum (SJAE) · exhaust hood spray = C-5/C-6 | doc 09 |
| GĐ‑87 | CCS loop C-5 dầu bôi trơn + hơi phụ trợ: nhiệt dầu + áp dầu bôi trơn + áp header hơi phụ trợ (v1.38, doc 09 §1): **19 → 22 loop live** (derived thuần, không state, 0 hồi quy thuần cộng). (1) **lube-oil-temp** (`TurbineGeneratorModel`): `TRB_LUBE_OIL_TEMP_01` = nền + ma sát gối theo tải − van CW cooler dầu `TRB_OIL_CW_VALVE_01` (reverse) giữ 45 °C. (2) **lube-oil-pressure**: `TRB_LUBE_OIL_PRESS_01` = nền + bơm/van `TRB_OIL_PUMP_CMD_01` (direct) giữ 0,2 MPa (bảo vệ màng dầu gối). (3) **aux-steam-header** (`FeedwaterTrainModel`): `FW_AUX_STEAM_PRESS_01` = van PRDS `FW_AUX_PRDS_VALVE_01` (direct) giữ 1,3 MPa cấp pegging deaerator/thổi bụi/phun sương dầu. Điểm vận hành: dầu 45 °C/van CW ~52% · áp dầu 0,2 MPa/bơm ~50% · header phụ trợ 1,3 MPa/PRDS ~50%; hạ tải 300 MW → ma sát giảm → van CW cooler dầu đóng bớt (giữ 45), áp dầu/header phụ trợ độc lập tải. Hiện D3-turbine (+4 ô dầu) · D3-feedwater-heatrate (+2 ô hơi phụ trợ). `T_OIL_AMB` 40 · `K_OIL_HEAT` 15 · `K_OIL_COOL` 12 · `P_OIL` 0,1+0,2·bơm · `P_AUX` 0,5+1,6·PRDS · SP 45 °C / 0,2 MPa / 1,3 MPa = [GIẢ ĐỊNH]. Test control-c5 (3) + zero-regression Stodola/tốc độ. Tới 25 còn 3 vòng: HP bypass · LP bypass · condenser air-ejector (SJAE) / condensate makeup = C-6 | doc 09 |
| GĐ‑88 | CCS loop C-6 — **CÁN MỐC 25 VÒNG**: hoàn thiện hệ H₂ máy phát + nước làm mát khép kín (v1.39, doc 09 §1): **22 → 25 loop live** (derived thuần, 0 hồi quy thuần cộng). (1) **generator-h2-temp** (`ElectricalModel`): `ELEC_H2_TEMP_01` = nền + nhiệt máy phát theo tải − van CW cooler H₂ `ELEC_H2_CW_VALVE_01` (reverse) giữ 40 °C. (2) **seal-oil-dp**: `ELEC_SEAL_OIL_DP_01` = nền + van seal oil `ELEC_SEAL_OIL_VALVE_01` (direct) giữ chênh áp seal oil−H₂ 0,08 MPa (dầu chèn chống rò H₂ ra ngoài). (3) **closed-cooling-water-temp** (`CondenserCWModel`): `COND_CCW_TEMP_01` = nền + tải nhiệt phụ trợ (dầu/H₂/stator/mẫu) theo tải − van CW bộ trao đổi `COND_CCW_CW_VALVE_01` (reverse) giữ 38 °C. Điểm vận hành: H₂ 40 °C/van CW ~36% · dP seal oil 0,08 MPa/van ~50% · CCW 38 °C/van ~36%; hạ tải 300 MW → nhiệt phụ trợ giảm → van CW H₂ & CCW đóng bớt (giữ setpoint), seal oil dP độc lập tải. Hiện D3-generator (+4 ô) · D3-condenser-cw (+2 ô). **Test control-c6 (4) gồm MỐC: `boilerControlLoops.length === 25`, id duy nhất, mọi vòng có `outTag`** + zero-regression net MW/hotwell. `K_H2_HEAT` 22 · `K_H2_COOL` 18 · `K_SEAL_OIL` 0,12 · `K_CCW_HEAT` 18 · `K_CCW_COOL` 15 · SP 40 °C / 0,08 MPa / 38 °C = [GIẢ ĐỊNH]. **CCS đạt mục tiêu 25 vòng điều khiển kín (governor→CCW), mỗi vòng có đường tác động vật lý thật + kiểm chứng đóng vòng.** Cải tiến sau (chưa làm): HP/LP bypass (cần khớp vật lý xả hơi lò/bình ngưng — hoãn để tránh hồi quy) · condenser air-ejector SJAE (chân không đã thuộc boiler) · exhaust hood spray (chỉ hoạt động tải rất thấp) | doc 09 |
| GĐ‑89 | Breadth §10 (batch a-1) BOP **Khí nén & khí điều khiển** (v1.40): `CompressedAirModel` (12→13 model, 20→21 màn hình + nav): 2 máy nén **lead–lag** (A lead luôn chạy, B trim load/unload theo dải áp 6,8–7,5 barg) → bình chứa → sấy khí điều khiển (dewpoint −40 °C) → header IA/SA. Nhu cầu khí = 25 + 55·tải Nm³/min. Tag `CA_*` (8), màn D3-compressed-air, có state (áp bình chứa + trạng thái trim) snapshot. Dải áp · suất máy nén 60 Nm³/min · dewpoint · hệ số nhu cầu = [GIẢ ĐỊNH]. | doc 10 §10 |
| GĐ‑90 | Breadth §10 (batch a-1) BOP **Dầu đốt khởi động & đỡ tải** (v1.40): `FuelOilModel` (→14 model, →22 màn hình): HFO (hâm 120 °C độ nhớt phun) + LDO (mồi). Dầu **chỉ đốt khi than < 60 t/h** (khởi động/đỡ lửa); ở tải than cao (211 t/h) lưu lượng dầu = 0, súng rút — đúng vận hành. HFO luôn hâm để bơm được. Tag `FO_*` (6), màn D3-fuel-oil, state (mức bồn + nhiệt HFO) snapshot. Ngưỡng 60 t/h · hâm 120 °C · áp cấp 25 barg · sức chứa bồn = [GIẢ ĐỊNH]. | doc 10 §10 |
| GĐ‑91 | Breadth §10 (batch a-1) BOP **Thải tro** (v1.40): `AshHandlingModel` (→15 model, →23 màn hình): tổng tro = than × **15% (Design Basis)**; tro đáy 20% (SSC/phễu) + tro bay 80% (phễu ESP theo trường A>B>C → silo → xe bồn hysteresis). Tag `ASH_*` (9), màn D3-ash-handling, state (mức silo + phễu tro đáy răng cưa) snapshot. Phân bổ đáy/bay · sức chứa silo · ngưỡng xả = [GIẢ ĐỊNH]. **Tiến độ breadth §10: 15 model · 23 màn hình · 25 loop (≥25 ✓). Còn thiếu tới ngưỡng nghiệm thu: ≥40 hệ · ≥70 màn · ≥8 sequence — BOP còn lại (soot blower · water treatment DM · chemical dosing · fire · HVAC · diesel gen · UPS/battery · switchyard) = batch a-2…** | doc 10 §10 |
| GĐ‑92 | HMI polish (b) — faceplate phủ đủ **25 vòng** (v1.41): trước chỉ 3 vòng trọng yếu có faceplate (drum-level, boiler-master, sh-temp). Nay `thermalFaceplates` = 3 chi tiết (KKS + alarm + trend đa tag) + **22 TỰ SINH từ `boilerControlLoops`** (một faceplate/vòng: PV/SP-hoặc-spTag/OP/mode/trend, tiêu đề tiếng Việt từ `desc`, EU suy từ tên tag). Mọi ô PV vòng trên mimic nay click mở được faceplate. DRY + tự phủ vòng tương lai. Đã kiểm: test faceplate-coverage (25 faceplate, assetId+pvTag duy nhất, đủ PV/OP/SP) + probe headless mở 3 faceplate tự sinh (mill/H₂/lube-oil) hiển thị PV sống. `assetId = PID-<loopId>`, EU cosmetic. Hoãn (b tiếp): alarm-shelf (shelve có lý do + hẹn giờ, ISA-18.2) — cần server+client, batch b-2 | doc 09/13 |
| GĐ‑93 | Tuning-calibration (c) — **lượng hoá độ lệch hiệu năng vs Design Basis** (v1.42): `CalibrationModel` đăng ký CUỐI (đọc KPI đã tính, ADDITIVE, 0 hồi quy). Phơi bày ĐỘ LỆCH (không che): **heat rate net sim 11.014 vs mốc 9.200 → +19,7%** · **hiệu suất net sim 32,7% vs 39% → −6,3 điểm%** · **CW cấp sim 31 °C (bầu ướt nhiệt đới) vs 19 °C ôn hoà → +12 °C** (đẩy back-pressure) · **khép cân bằng NL 100% → lệch 0** (kiểm toán độc lập nguồn ĐÚNG). Tag `PLANT_CAL_*` (7) + màn D2-calibration (sim\|mốc\|độ lệch) + nav. Đây là bước ĐẦU của "calibrate bằng heat-balance thật": ĐO trước khi chỉnh. **Đóng gap heat rate/hiệu suất = đổi hằng số coal→steam→MW (điểm vận hành than 211 t/h @ 448 MW) → RỦI RO HỒI QUY CAO trên nhiều test → HOÃN tới khi có số vận hành thật** (đúng nguyên tắc: không bịa số, GĐ-67/68). Gap CW nhiệt đới là dữ kiện site thật, không phải lỗi. Test calibration (2) + probe màn D2. | doc 10 §10 / doc 25 |
| GĐ‑94 | Vòng hoãn (d) — **SJAE hút khí + HP turbine bypass** (v1.43): `BypassAirRemovalModel`, **25 → 27 vòng** (25 lõi vận hành + 2 bảo vệ/khởi động). (1) **sjae-air-removal** (LIÊN TỤC ở tải): `COND_O2_01` = nền − van hút khí SJAE `COND_SJAE_VALVE_01` (reverse) giữ O₂ hoà tan ~7 ppb (van ~72%). (2) **hp-bypass-pressure** (KHỞI ĐỘNG/TRIP): van `BLR_HP_BYPASS_VALVE_01` xả hơi SH khi áp > 18,5 MPa (reverse) → `BLR_HP_BYPASS_FLOW_01`. **Kiểm chứng actuator-path thật qua trip**: ở tải áp SH 17,5 < 18,5 → van 0/xả 0 (0 hồi quy); **trip turbine → áp SH tăng 19,2 MPa → van mở 9,1% → xả 72,7 t/h bảo vệ lò** (nối vào physics trip sẵn có). Van bypass KHÔNG trừ hơi turbine ở điểm vận hành (=0) → 0 hồi quy. Hiện D3-condenser-cw (+2 ô SJAE) · D3-turbine (+2 ô bypass). Faceplate tự sinh khử trùng theo pvTag (hp-bypass chung áp SH với boiler-master → 1 faceplate). `O2_BASE` 25 ppb · `K_SJAE` 25 · `HP_BYPASS_CAP` 800 t/h · ngưỡng 18,5 MPa = [GIẢ ĐỊNH]. Test bypass-sjae (3). **Còn hoãn: LP bypass (cần tag áp reheat riêng) · exhaust hood spray (chỉ chạy tải rất thấp).** | doc 10 §6/§7 |
| GĐ‑95 | Breadth §10 (batch a-2) BOP **Thổi bụi bề mặt truyền nhiệt** (v1.44): `SootBlowerModel` (→18 model, →25 màn hình + nav): tro/xỉ bám bề mặt truyền nhiệt → chỉ số bám `SB_FOULING_INDEX_01` tăng ∝ than-tro (6%/giờ ở than chuẩn 211 t/h); vượt ngưỡng 45% → khởi động **chu trình thổi** quét lần lượt 4 vùng (vách buồng lửa 24 · SH/RH 16 · bộ hâm 8 · bộ sấy gió 2 = 50 máy thổi), mỗi vùng thổi 6 phút, hạ bám về mức dư 5%, tiêu **12 t/h hơi ký sinh** (header ~30 barg, sụt 2,5 khi thổi). Tag `SB_*` (9), màn D3-soot-blower, state (chỉ số bám + tiến trình chu trình) snapshot. **`SB_GAS_EXIT_TEMP_DELTA_01` chỉ ƯỚC LƯỢNG read-only** (bám → khói ra nóng, 0,9 °C/%) — KHÔNG tác động `FlueGasAirModel` ⇒ 0 hồi quy. Malfunction `sootblower-fault` (máy thổi kẹt → thổi không làm sạch → bám leo thang, OTS). 2 alarm live (SB-FOULING-HI P3 @55% · SB-STEAM-PRESS-LO P2 @20 barg) — ngưỡng ngoài dải vận hành thường → 0 hồi quy trạng thái alarm. Tốc độ bám · ngưỡng chu trình · số máy thổi/vùng · thời lượng/vùng · suất hơi thổi · hệ số bám→nhiệt = [GIẢ ĐỊNH]. **Tiến độ breadth §10: 18 model · 25 màn hình · 27 loop. Còn thiếu tới ≥70 màn: water treatment DM · chemical dosing · fire · HVAC · diesel gen · UPS/battery · switchyard 500 kV = batch a-3…** | doc 10 §10 |
| GĐ‑96 | HMI polish (b-2) — **alarm-shelf ISA-18.2** (v1.44): hoàn tất nợ ở GĐ-92 (shelve có lý do + hẹn giờ, cần server+client). `AlarmEngine` trước đã có `shelve()` (tự bung ≤ 8 h) nhưng **bỏ qua `reason`**, không liệt kê được, không bung thủ công. Nay: (1) engine lưu `reason`+`user`+`shelvedAt`, **chặn reason rỗng** (ISA-18.2 bắt buộc lý do), thêm `getShelved(nowMs)` (kèm lý do + thời gian còn lại) + `unshelve()` (bung thủ công trước hạn → về Normal, tái kích nếu điều kiện còn); auto-bung hết hạn xoá metadata. (2) `ThermalRuntime` expose `shelveAlarm/unshelveAlarm/shelvedAlarms` + ghi nhật ký sự kiện. (3) Server: lệnh `shelve`/`unshelve` (RBAC action `ack` = Operator+, KHÔNG 2-bước — shelve là chức năng operator), đưa `shelved` vào message alarms, broadcast report-by-exception theo tập active+shelved. (4) Client: nút 🔕 mỗi alarm row → modal (lý do bắt buộc + thời hạn 30′/1h/4h/8h), badge Shelve + modal danh sách shelved với nút Bung. **ADDITIVE — chữ ký `shelve()` cũ giữ nguyên (test engine cũ xanh); không alarm mặc định nào bị shelve → 0 hồi quy.** Test: engine +2 (getShelved/unshelve) · app +2 (round-trip runtime) · probe headless drive UI thật shelve→unshelve badge đồng bộ, 0 JS error. | doc 05-03 / doc 08 |
| GĐ‑97 | Vòng hoãn (d) — **LP turbine bypass**, **27 → 28 vòng** (v1.44): hoàn tất vòng hoãn cuối ở GĐ-94. PV = `TRB_HRH_PRESS_01` (áp hot reheat — ĐÃ có sẵn ở `ReheatCycleModel`, không cần thêm tag mới như lo ngại ban đầu). Vòng **lp-bypass-pressure** (reverse) xả hot reheat → BÌNH NGƯNG khi áp reheat > **4,0 MPa** (đo được: điểm vận hành HRH **3,80 MPa** < ngưỡng → van ~0; trip đẩy HRH lên **4,27 MPa** > ngưỡng → van mở). `BypassAirRemovalModel` mở rộng ADDITIVE: đọc van `TRB_LP_BYPASS_VALVE_01` → `TRB_LP_BYPASS_FLOW_01` (cap 1.200 t/h) + `TRB_LP_BYPASS_OPEN_01`. **Kiểm actuator-path thật qua trip** (giống HP bypass): tải van ~0,1%/xả ~1 t/h (0 hồi quy); trip → van mở > 3%/xả > 20 t/h. Readout D3-turbine +2 ô. Faceplate tự sinh (pvTag HRH press duy nhất → 1 faceplate). kp 40/ki 2 · ngưỡng 4,0 MPa · cap 1.200 t/h = [GIẢ ĐỊNH]. Test bypass-sjae +2 (LP đóng ở tải · LP mở khi trip) · probe D3-turbine readout sống. **Còn hoãn duy nhất: exhaust hood spray (chỉ chạy tải rất thấp).** | doc 10 §6/§7 |
| GĐ‑98 | Calibration (c-2) — **ĐO thêm điều kiện hơi/chân không được điều khiển vs Design Basis** (v1.44): tiếp GĐ-93 ("ĐO trước khi chỉnh"), KHÔNG đổi vật lý, KHÔNG bịa số (tôn trọng QUYẾT ĐỊNH M-06/C1 giữ 33% trung thực). `CalibrationModel` mở rộng ADDITIVE 8 tag `PLANT_CAL_{MST,HRH,MSP,VAC}_{TGT,DEV}_01`: đo nhiệt hơi chính (vs 541 °C) · nhiệt hot reheat (vs 541 °C) · áp hơi chính (vs 17,5 MPa) · chân không bình ngưng (vs 5,4 kPa) — **mọi mốc từ Phụ lục A**. Kết quả (đo được): độ lệch **~0** ở cả 4 (hơi chính −0,1 °C · hot reheat −0,2 °C · áp −0,01 MPa · chân không 0,0 kPa) trong khi heat rate vẫn +19,5% / η −6,3đ%. **Ý nghĩa: KHU BIỆT nguyên nhân — sim giữ ĐÚNG mọi điều kiện hơi/chân không thiết kế ⇒ gap heat-rate/η KHÔNG do sai điều kiện hơi mà do hằng số coal→steam→MW** (khẳng định chẩn đoán M-06: chỉ tái dẫn xuất TOÀN BỘ điểm vận hành bằng heat-balance thật mới đóng được gap, không phải tweak). Màn D2-calibration +4 hàng (sim\|mốc\|lệch). Test calibration +1 (điều kiện hơi ≈ mốc + tương phản gap heat-rate) · probe D2 6 tile mới sống, 0 overlap. | doc 10 §10 / doc 25 |
| GĐ‑99 | Breadth §10 (batch a-3) BOP **Xử lý nước khử khoáng DM** (v1.45): `WaterTreatmentModel` (→19 model): dây chuyền cation→khử khí→anion→mixed bed→bồn DM→bơm bù. Nhu cầu bù ≈ **1,2% lưu lượng hơi** + hơi thổi bụi không hồi; sản xuất DM theo hysteresis giữ mức bồn; nhựa tích tải theo thông lượng → vượt breakthrough 85% thì độ dẫn/silica tăng vọt → **tái sinh** (offline 1 dây chuyền 90′, nạp lại). Tag `WT_*` (9), màn D3-water-treatment + nav, state (mức bồn·tải nhựa·tái sinh) snapshot. Malfunction `dm-resin-fault` (nhựa hỏng→rò ion). Tỷ lệ bù·sức chứa·ngưỡng breakthrough·thời gian tái sinh = [GIẢ ĐỊNH]. | doc 10 §10 |
| GĐ‑100 | Breadth §10 (batch a-3) BOP **Nguồn điện khẩn cấp** (v1.45): `EmergencyPowerModel` (→20 model): (1) Diesel khẩn cấp (EDG) dừng-sẵn sàng, tự khởi động+mang tải khi mất điện tự dùng; (2) UPS+ắc-quy **DC 220/110 V** nạp nổi (SOC~100%), mất điện→chạy ắc-quy (SOC giảm ~25%/h). Đọc GEN_MW>10 suy lưới tự dùng còn. Tag `EDG_*`/`UPS_*` (11), màn D3-emergency-power + nav, state (SOC·dầu·latch) snapshot. Malfunction `station-blackout`. Suất EDG·autonomy ắc-quy·tốc độ xả/nạp = [GIẢ ĐỊNH]. | doc 10 §7 |
| GĐ‑101 | Breadth §10 (batch a-3) BOP **Trạm phân phối 500 kV** (v1.45): `SwitchyardModel` (→21 model): đọc `ELEC_GRID_MW_01` (xuất lưới sau GSU) + `TRB_SPEED_01` (→tần số) → 2 đường dây 500 kV chia đều, điện áp thanh cái (~500 kV, sụt nhẹ theo tải), dòng đường dây I=P/(√3·V·pf), máy cắt tổng. Đại số thuần (latch sự cố). Tag `SY_*` (10), màn D3-switchyard + nav. Malfunction `line-trip` (1 đường dây cắt→đường còn lại gánh toàn tải, dòng ×2). Số đường dây·điện áp danh định·pf = [GIẢ ĐỊNH]. Neo Design Basis: lưới 500 kV/50 Hz·GSU 720 MVA. | doc 10 §7 |
| GĐ‑102 | Breadth §10 (batch a-3) BOP **HVAC** (v1.45): `HvacModel` (→22 model): điều hoà phòng điều khiển (nhiệt ~23 °C, quán tính τ 120 s) + làm mát phòng tủ điện (tải nhiệt I²R theo GEN_MW) + nước lạnh chiller 7 °C + ΔP lọc gió. Tag `HVAC_*` (8), màn D3-hvac + nav, state (nhiệt phòng điều khiển) snapshot. Malfunction `hvac-chiller-trip` (mất làm mát→nhiệt/ẩm tăng). Nhiệt/ẩm đích·tải chiller·ΔP = [GIẢ ĐỊNH]. | doc 10 §10 |
| GĐ‑103 | Breadth §10 (batch a-3) BOP **Hệ chữa cháy** (v1.45): `FireFightingModel` (→23 model): vòng ống chính giữ ~9 barg bằng bơm jockey; nhu cầu chữa cháy→áp tụt→bơm chính (điện) chạy, dự phòng bơm diesel; bồn nước 2.000 t + bình bọt. Tag `FIRE_*` (8), màn D3-fire-fighting + nav, state (áp vòng ống·mức bồn·latch) snapshot. Malfunction `fire-detected` (báo cháy+bơm chính+rút nước bồn). Áp vòng ống·dung tích bồn·ngưỡng khởi bơm = [GIẢ ĐỊNH]. **Tiến độ breadth §10 sau batch a-3: 23 model · 30 màn live · 28 loop. Hệ BOP §10 đã phủ: soot blower·water treatment·emergency power·switchyard·HVAC·fire fighting (+ khí nén·dầu đốt·thải tro trước đó).** | doc 10 §10 |
| GĐ‑104 | Breadth §10 (batch a-3 bổ sung) BOP **Hoá chất điều hoà chu trình** (v1.46): `ChemicalDosingModel` (→24 model): amoniac điều pH nước cấp (AVT chống ăn mòn CO₂) · khử oxy (hydrazine) · phosphate xử lý nội bộ bao hơi; bơm định lượng ∝ lưu lượng hơi. Hoá lý đích: pH nước cấp ~9,3 · pH bao hơi ~9,6 · phosphate ~5 ppm · độ dẫn cation hơi ~0,15 µS/cm (tinh khiết). Tag `CHEM_*` (8), màn D3-chemical-dosing + nav, state (mức bồn hoá chất) snapshot. Malfunction `chem-dosing-fail` (mất điều hoá→pH tụt vùng ăn mòn + độ dẫn cation tăng). pH đích·liều·phosphate·độ dẫn = [GIẢ ĐỊNH]. **Danh mục BOP §10 nay PHỦ ĐỦ: khí nén·dầu đốt·thải tro·soot blower·water treatment·emergency power·switchyard·HVAC·fire fighting·chemical dosing. Tiến độ: 24 model · 31 màn live · 28 loop.** | doc 10 §10 |
| GĐ‑105 | Rationalize **6+1 điều kiện malfunction BoP → AlarmDef first-class** (doc 08 §5.1): 7 `AlarmDef` mới trong `boiler-alarms.ts` (đủ deadband + on/off delay + consequence/corrective), neo tag process thật, setpoint giữa NORMAL/FAULTED đã ĐO trên runtime: `WTP-DM-COND-HI` (WT_PRODUCT_COND_01 H 0,20 µS/cm, P2) · `EMG-STATION-BLACKOUT` (UPS_ON_BATTERY_01 H 0,5, P1) · `SWY-LINE-TRIP` (SY_LINES_INSERVICE_01 L 1,5, P2) · `HVAC-CR-TEMP-HI` (HVAC_CR_TEMP_01 H 24,5 °C, P2) · `FIRE-DETECTED` (FIRE_ALARM_ACTIVE_01 H 0,5, P1) · `CHEM-FW-PH-LO` (CHEM_FW_PH_01 L 8,8, P2) · `CA-IA-PRESS-LO` (CA_IA_HEADER_PRESS_01 L 5,5 barg, P1). `CompressedAirModel` (GĐ‑89) nay mang malfunction `instrument-air-loss` (máy nén cô lập → bình chứa về sàn → IA sập). ADDITIVE — chỉ thêm AlarmDef + 1 malfunction, 0 hồi quy (vận hành ổn định 0/7 alarm nổi). Setpoint/priority/delay = [GIẢ ĐỊNH], hiệu chỉnh khi có bảng logic bảo vệ thật. Kiểm: `bop-alarm-rationalization.test.ts` (9). | doc 08 §5.1 |
| GĐ‑106 | Chiều sâu physics phía điện — **AVR / hệ kích từ máy phát** (v1.47): `AvrExcitationModel` (ADDITIVE, đăng ký SAU electrical): bộ điều chỉnh điện áp tự động giữ **điện áp đầu cực 20 kV (1,0 pu)** bằng **dòng kích từ** → chi phối phản kháng. Đọc `GEN_MVAR_01` thực (KHÔNG override) → dòng kích từ = nền 1.500 A + 5,4 A/MVAr (điểm vận hành Q≈278 → field≈3.000 A, kích thích ~75%, khớp máy phát 667 MVA/0,9 PF). Vòng kích từ có quán tính τ 1,5 s. Tag `ELEC_{TERM_VOLT,TERM_VOLT_PU,AVR_SETPOINT,FIELD_CURRENT,FIELD_VOLTAGE,EXCITATION,REACTIVE_AVR,AVR_MODE}_01` (8), màn D3-avr-excitation + nav, state (dòng kích từ + điện áp cực) snapshot. Malfunction: `grid-undervoltage` (lưới sụt 0,94 pu → AVR tăng kích từ + huy động MVAr đỡ áp, GIỮ điện áp cực) · `avr-manual` (khoá kích từ → điện áp cực trôi theo lưới, mất điều áp — OTS). **0 hồi quy: chỉ đọc GEN_MVAR/GEN_MW, sinh tag ELEC_* mới.** Dòng kích từ nền/trần 4.000 A · hệ số kích-từ→MVAr · R cuộn kích 0,12 Ω · hệ số đỡ áp 250 MVAr/pu = [GIẢ ĐỊNH]. Test: plugin avr-excitation (4) + app avr-runtime (2, gồm 0-hồi-quy MW/MVAr) · probe D3-avr 8 readout sống, 0 overlap. | doc 10 §7 |
| GĐ‑107 | Chiều sâu physics — **DRAIN CASCADE bình gia nhiệt** (v1.48): `FeedwaterDrainsModel` (ADDITIVE, đăng ký SAU feedwater-train). Phía VỎ bình hồi nhiệt: hơi trích ngưng → drain CASCADE HP3→HP2→HP1→deaerator · LP4→…→LP1→bình ngưng. Đọc nhiệt đầu ra bình (FW_* đã có) → nhiệt drain mỗi bình = nước cấp VÀO bình + **DCA 5,5 °C** · **TTD 3 °C** (tăng ở non tải) · mức drain (van thường giữ 50%, cao > 80% → **van xả khẩn** đổ về bình ngưng, mất hồi nhiệt). Tag `FWH_*` (12), màn D3-fwh-drains + nav, state (mức drain HP/LP + latch xả khẩn) snapshot. Malfunction `heater-drain-high`. **0 hồi quy: chỉ đọc FW_*, sinh FWH_* mới** (test app kiểm nhiệt econ inlet không đổi). TTD/DCA/tỉ lệ drain 9%/ngưỡng xả khẩn = [GIẢ ĐỊNH]. Test: plugin feedwater-drains (4) + app (2). | doc 10 §6 |
| GĐ‑108 | Scenario/SFC — **cold-start → sync → ramp đầy tải ĐẦY ĐỦ** (v1.49): thêm **2 SFC tiền đề cold-start** (`SequenceEngine`, doc 09): `condenser-vacuum-raise` (chạy CW → cấp hơi chèn trục → khởi động SJAE → kéo chân không tới định mức, transition `TRB_COND_VACUUM_01 ≤ 10 kPa`) · `pressure-raising` (mở drain quá nhiệt → soak làm ấm → nâng áp có kiểm soát, transition `BLR_MSTM_SH_PRESS_01 ≥ 16 MPa` → đóng drain) → **9 → 11 SFC**. Thêm **kịch bản `cold-start-to-full-load`** (10 pha): chân không → thông gió → mồi lửa → điền nước → nâng áp/soak → mill → quay turbine → hoà lưới → ramp 600 MW → giữ tải phối hợp → **1 → 2 scenario** (bổ trợ kịch bản startup→coastdown có trip). Tag lệnh mới `COND_CW_PUMP_CMD·TRB_GLAND_SEAL_CMD·COND_SJAE_START_CMD·COND_VACUUM_READY·BLR_SH_DRAIN_CMD·BLR_WARMUP_SOAK_CMD·BLR_PRESS_RAISE_CMD` (orchestration, như SFC hiện có). Ngưỡng chân không/áp · holdMs/soak = [GIẢ ĐỊNH] (tinh chỉnh theo quy trình khởi động thật). Test: plugin cold-start (4) + app cold-start-full-load (1) + cập nhật scenario count → 2. Sim warm-start nên cold-start mang tính ĐIỀU PHỐI (physics cold-start từ nguội đầy đủ = pha sau, như GĐ-46). | doc 09/22 |
| GĐ‑109 | Chiều sâu physics — **bảo vệ máy phát ANSI/IEEE C37.102** (v1.50): `AnsiProtectionModel` (ADDITIVE, đăng ký SAU avr+switchyard). Mô phỏng 5 chức năng rơle: **87G** vi sai (nền 0,5% · chạm chập 45% · pickup 10%) · **40** mất kích từ (biên suy từ dòng kích từ, pickup < 25%) · **46** thứ tự nghịch I₂ (nền 2% · giới hạn liên tục 8%) · **81** tần số (dải 49–51 Hz) · **24** V/Hz (=V_pu/(f/50), pickup > 110%). Đọc GEN_MVAR/dòng kích từ (AVR)/điện áp cực/tần số (switchyard) → pickup + cờ trip + `ANSI_PROT_HEALTHY`/`ANSI_TRIP_ANY`. **READ-ONLY tuyệt đối: rơle chỉ giám sát + phát cờ, KHÔNG ghi tag process/ngắt máy cắt** (nối cắt thật = tầng C&E, để sau). Tag `ANSI_*` (12), màn D3-ansi-protection + nav. Malfunction: `gen-internal-fault` (87) · `gen-loss-field` (40) · `gen-unbalance` (46); 81/24 theo đại lượng thực. Không latch (pickup theo điều kiện → OTS clear là phục hồi). Ngưỡng pickup/định thời = [GIẢ ĐỊNH] (relay setting sheet thật thay khi có). Test: plugin ansi-protection (6) + app (2, gồm 0-hồi-quy MW/MVAr) · probe D3-ansi 12 readout sống, 0 overlap. | doc 10 §7 |
| GĐ‑110 | Chiều sâu physics — **NỐI drain cascade vào CÂN BẰNG NHIỆT** (per-heater sâu hơn, v1.51): `RegenBalanceModel` (ADDITIVE + GUARD 0 hồi quy). Lượng hoá tác động nhiệt khi drain bình gia nhiệt bị CHUYỂN HƯỚNG (van xả khẩn đổ thẳng bình ngưng thay vì cascade sưởi nước cấp): đọc `FWH_DRAIN_TO_COND_01` vs cascade nền 9% hơi → lưu lượng chuyển hướng → **tổn thất hồi nhiệt** (MWth) → **hạ nhiệt nước cấp vào econ** → **phạt heat rate** (%). **GUARD: vận hành bình thường van xả khẩn ĐÓNG → drain=nền → tổn thất=0 → nước cấp econ hiệu dụng=danh nghĩa → `PLANT_CYCLE_HR_EFF`=`PLANT_CYCLE_HR` (KHÔNG đổi ⇒ 0 hồi quy).** Chỉ khi `heater-drain-high` mở xả khẩn → phơi bày mức phạt. **KHÔNG ghi đè FW_ECON_INLET/PLANT_CYCLE_HR — sinh tag '\_EFFECTIVE/\_EFF' MỚI** (read-only với physics lõi, tôn trọng bài học M-06: không đụng điểm vận hành/enthalpy). Tag `PLANT_REGEN_LOSS_MW·PLANT_FW_TEMP_DEPRESSION·FW_ECON_INLET_EFFECTIVE·PLANT_HR_REGEN_PENALTY·PLANT_CYCLE_HR_EFF·PLANT_REGEN_HEALTHY_01` (6), màn D3-regen-balance + nav. cp 4,4 · Δh_boiler 2145 · cascade nền 9% · ΔT hồi nhiệt LP = [GIẢ ĐỊNH]. Test: plugin regen-balance (2) + app (2, gồm coupling end-to-end xả khẩn → phạt HR > 0). | doc 10 §6 |
| GĐ‑111 | Scenario/SFC — **vận hành two-shift & island mode** (v1.52): thêm **2 SFC** (`generator-desync`: giảm tải → mở máy cắt đầu cực `GEN_BREAKER_CMD=0` → chuyển cấp tải tự dùng `UNIT_HOUSE_LOAD_CMD=1`; `boiler-bank`: ủ lò nóng-chờ `BLR_BANK_CMD/BLR_HOT_STANDBY_CMD`) → **11 → 13 SFC**. Thêm **2 kịch bản** → **2 → 4 scenario**: `two-shift-cycle` (10 pha: ngày → chiều giảm tải → tách lưới → ủ lò → qua đêm → thoát ủ → mill restart → hoà lại → ramp về tải) · `grid-island-runback` (8 pha: đầy tải → mất lưới `line-trip` → island về tự dùng → giữ island governor/AVR → lưới phục hồi → hoà lại → ramp). Tag lệnh mới `UNIT_HOUSE_LOAD_CMD·BLR_BANK_CMD·BLR_HOT_STANDBY_CMD·BLR_BANK_LEVEL_CMD` (orchestration). holdMs/mức tải = [GIẢ ĐỊNH]. Test: plugin two-shift-island (3) + app (2, chạy end-to-end 2 kịch bản qua runtime) + cập nhật scenario count → 4. | doc 09/22 |
| GĐ‑112 | Nối bảo vệ ANSI vào **Cause&Effect ACTUATION** thật (v1.53): thêm ma trận C&E thứ 3 `generator-protection` (2 → 3 ma trận) — nguyên nhân = cờ trip rơle ANSI (`ANSI_87_TRIP` · `ANSI_40_PICKUP` · `ANSI_46_PICKUP`) → hệ quả CHỐT: `TRB_TRIP`=1 (turbine sim đọc → MW=0, coast-down) + `GEN_BREAKER_TRIP`=1 + `GEN_FIELD_SUPPRESS`=1. Rơle không còn chỉ báo cờ mà THỰC SỰ cắt tổ máy end-to-end. **Chỉ LỖI ĐIỆN NỘI BỘ 87/40/46 lockout; 81 (tần số) giữ làm chỉ thị, KHÔNG nối lockout** (tránh chốt lại do underfrequency lúc coast-down). **0 hồi quy: điểm vận hành mọi cờ ANSI = 0 → không nguyên nhân hoạt; coast-down MFT thường không kích 87/40/46.** Cập nhật test cũ ansi-protection (rơle-model vẫn read-only; ACTUATION qua C&E) + 2 assertion đếm ma trận (2→3, cause-effect & server). Test: app ansi-ce-trip (3: op 0-chốt · 87 → turbine trip → MW sập + máy cắt + triệt kích từ · reset sau gỡ sự cố). | doc 09 §4 / doc 10 §7 |
| GĐ‑113 | Chiều sâu phát thải — **CEMS mở rộng Hg + CO** (v1.54): `CemsHgCoModel` (ADDITIVE, đăng ký SAU emissions). (1) Thuỷ ngân: Hg than 0,1 g/t → thu hồi CO-BENEFIT ESP+FGD 65% + phun THAN HOẠT TÍNH ACI +25% (→90%), Hg⁰ thoát ống khói (µg/Nm³). (2) CO: nền 30 mg/Nm³ ở O₂ danh định, tăng theo mũ khi O₂ tụt (thiếu gió→cháy không hết); quy về 6% O₂ (pháp lý); suy hiệu suất cháy. Đọc than/`EMI_FG_VOLUME`/`BLR_FLUE_O2` → `CEMS_*` (7), màn D3-cems-hg-co + nav. Malfunction `hg-sorbent-loss` · `co-excursion`. **0 hồi quy: sinh tag CEMS_* mới, không đụng tag emissions** (test kiểm CO₂/bụi không đổi). Hàm lượng Hg than · tỉ lệ thu hồi · tương quan CO–O₂ = [GIẢ ĐỊNH]. Test: plugin cems-hg-co (6) + app (2). | doc 10 §6 |
| GĐ‑114 | Chiều sâu physics — **bám bẩn & lọt khí ĐỘNG** (v1.55): `FoulingAirIngressModel` (ADDITIVE, đăng ký SAU soot-blower). 2 quá trình suy giảm THEO THỜI GIAN (stateful): (1) **bám bộ sấy gió** tích ∝ than-tro (4%/giờ), LÀM SẠCH khi chu trình thổi bụi chạy (đọc `SB_CYCLE_ACTIVE_01`) → ΔP gió-khói · ΔT khói ra · hiệu quả AH. (2) **lọt khí bình ngưng** tiến hoá tới đích (nền 5 scfm / sự cố 55) với hằng thời gian 0,5h → biên SJAE (năng lực 40 scfm) · phạt chân không khi vượt năng lực. Tag `FA_*` (8), màn D3-fouling-air + nav, state (bám AH + suất lọt khí) snapshot. Malfunction `ah-fouling-accelerated` · `condenser-air-leak`. **0 hồi quy: đọc-only, penalty là ƯỚC LƯỢNG chẩn đoán — không đổi tag lõi** (test kiểm chân không lõi không đổi). Tốc độ bám/làm sạch · suất lọt khí/năng lực SJAE · hệ số penalty = [GIẢ ĐỊNH]. Test: plugin fouling-air-ingress (5) + app (2). | doc 10 §6 |
| GĐ‑119 | Hiệu năng hệ alarm — **KPI EEMUA-191 tag hoá** (v1.58): runtime (sở hữu alarm engine) ĐỌC trạng thái alarm mỗi bước → sinh tag `ALM_*` (9): suất/10 phút · đỉnh suất · flood (>10/10ph) · đang hoạt động · **đứng lâu >10 phút (standing/stale)** · chưa xác nhận · P1 active · bad-actor cao nhất · **đạt EEMUA (suất≤10 ∧ không flood ∧ standing≤5)**. Màn D2-alarm-performance (9 ô) + nav. **READ-ONLY với process: chỉ đọc alarm engine, KHÔNG đụng tag process (0 hồi quy)** — rate/flood/bad-actor từ `AlarmEngine.kpi()` có sẵn; standing/unack/P1/đỉnh tính ở runtime. Ngưỡng "chịu được" 10 alarm/10ph · standing tối đa 5 · cửa sổ 10 phút = [GIẢ ĐỊNH] (theo EEMUA-191). Test: app mills-governor-alarmkpi (KPI nền yên + bơm sự cố → suất tăng). | doc 08 §KPI |
| GĐ‑118 | Chiều sâu physics — **điều tốc droop & đáp ứng tần số sơ cấp** (v1.58): `GovernorDroopModel` (ADDITIVE, đăng ký SAU pssStabilizer). Đặc tuyến droop 5% + deadband ±30 mHz + PFR: ngoài deadband ΔP=−(Δf/f_n)/R×MCR → điều tốc mở/đóng van góp công suất đỡ/giảm tần số. Tần số lưới NỘI (small-signal sự kiện): under-freq→49,8 Hz (PFR ~+40 MW), over-freq→50,2 Hz (PFR âm). Cặp đôi với AVR/PSS (điện áp/dao động) — điều tốc lo tần số/tải. Tag `GOV_*` (9), màn D3-governor-droop + nav, state (tần số lưới nội) snapshot. Malfunction `grid-underfrequency` · `grid-overfrequency` · `governor-oos` · `droop-mistuned` (2%). **0 hồi quy: chỉ sinh GOV_*, PFR là tín hiệu điều tốc SẼ ra lệnh, không ghi GEN_MW/GEN_FREQ** (test kiểm điểm vận hành trong deadband, PFR≈0). Droop/deadband/khả năng PFR ±60 MW/đích sự kiện = [GIẢ ĐỊNH]. Test: plugin governor-droop (6) + app (1). | doc 10 §7 |
| GĐ‑128 | Chiều sâu physics — **TSE ứng suất nhiệt rotor turbine** (v1.62): `TurbineStressModel` (ADDITIVE, đăng ký SAU turbine). Chênh nhiệt BỀ MẶT–TÂM rotor sinh ứng suất nhiệt: bề mặt bám hơi nhanh (τ 20s), tâm trễ (τ 260s) → khi đổi tải/nhiệt nhanh ΔT vọt → ứng suất → tiêu hao tuổi thọ mỏi chu kỳ thấp. TSE thắt TỐC ĐỘ TẢI cho phép theo biên ứng suất (supervisory thật). Đọc GEN_MW_01 + BLR_MSTM_SH_TEMP_01 → 8 tag `TSE_*` (nhiệt bề mặt/tâm · ΔT · ứng suất% · biên · ramp limit MW/ph · tuổi thọ · lành mạnh). Khởi tạo LƯỜI (rotor thấm nhiệt điểm vận hành → ΔT khởi điểm 0). Malfunction `fast-startup` · `thermal-shock`. **0 hồi quy: chỉ sinh TSE_*, không đổi tag turbine lõi** (test kiểm GEN_MW không đổi). **Đổi tải 448→560 → ΔT ~42°C → TSE tự hạ ramp limit 30→14 MW/ph** (bài dạy OTS: tôn trọng ramp). Màn D3-turbine-stress + nav, state (nhiệt + tuổi thọ) snapshot. Chênh cho phép 60°C · ramp max 5%/ph · τ bề mặt/tâm · hệ số tuổi thọ = [GIẢ ĐỊNH]. Test: plugin turbine-stress (6) + app (4). | doc 10 §7 |
| GĐ‑127 | Nền tảng — **app plugin #2 chạy được (digital twin thứ 2)** (v1.61): `apps/water-runtime` — app tổ hợp WaterTankModel + RoMembraneModel + control loop + AlarmEngine trên ĐÚNG engine chung (@idtp/engines) mà thermal-runtime dùng. Server WS phát tag delta + alarm; HMI riêng (readout bể + màng RO + panel alarm + nút sự cố). **Chứng minh "mọi nhà máy chỉ là plugin" bằng nhà máy KHÁC CHẠY THẬT**: 0 dòng kernel/engine riêng cho nước — chỉ import plugin #2 + engines. Warmup 5000 bước để loop mức bể hội tụ SP 60% trước khi phục vụ. Test: runtime (4) + server (1) + E2E smoke headless (permeate/thu hồi/mức render + thủng màng → alarm panel). Bộ test 499 → 504; monorepo 6→7 app/plugin có app chạy. | doc 00 §5.4 / doc 24 |
| GĐ‑126 | Chất lượng — **hardening & coverage** (v1.60, E): (1) Test TRỰC TIẾP `SimulationHost` (engines) — orchestrator ISimModel trước chỉ phủ gián tiếp qua app/plugin (**2% → phủ đầy**: register/init · step publish · freeze · inject/clear đích + injectAll/clearAll · snapshotAll/restoreAll + bỏ qua id thiếu). **Engines stmts 91,7% → 94,1%.** (2) Test load-following **448→560→400 MW** — toàn hệ (lõi + 20+ model chiều sâu) BÁM tải, mọi tag hữu hạn, than tăng theo tải, không trip giả. (3) OTS round-trip — freeze dừng tiến; snapshot/restore khôi phục trạng thái model chiều sâu sau khi tiêm sự cố (mill-trip + air-leak). Test: engines simulation-host (5) + app load-following-ots (3). Bộ test tổng 479 → 499 xanh. | doc 05-05 / doc 24 |
| GĐ‑125 | Nền tảng — **plugin #2 mở rộng độc lập** (v1.60, C): thêm `RoMembraneModel` (màng thẩm thấu ngược — quá trình VẬT LÝ KHÁC: permeate/reject · thu hồi ~75% · khử muối ~99% · ΔP bám màng · độ dẫn permeate) + 2 alarm RO (WTP-RO-COND-HI/DP-HI) + màn D3-wtp-ro vào `water-treatment-demo`. **Chỉ chạm `plugins/water-treatment-demo/*` — 0 dòng sửa `packages/*` & `apps/*`** (kiểm bằng phạm vi thay đổi). Củng cố luận điểm "mọi nhà máy chỉ là plugin": plugin lớn thêm mà kernel/engine bất động; RO + alarm chạy trên ĐÚNG SimulationHost/AlarmEngine chung. Về persistence: adapter pg/mqtt THẬT đã c8-ignore (deploy-only), seam DI phủ đầy bằng adapter GIẢ ở persistence.test.ts — không thêm việc. Số liệu RO demo (áp 15 bar · thu hồi 75% · khử muối 99,2%) = [GIẢ ĐỊNH]. Test: plugin water ro-membrane (4) → bộ plugin #2 4→8. | doc 00 §5.4 |
| GĐ‑124 | Vận hành alarm — **rationalization ISA-18.2** (v1.60, B): lớp KHAI BÁO `thermalAlarmRationalization` (19 bản ghi, cạnh AlarmDef — thêm nguyên nhân gốc · thời gian phản ứng · căn cứ ưu tiên · đã duyệt). `buildAlarmRationalization()` gộp AlarmDef × bản ghi → báo cáo master + độ phủ + **phân bố ưu tiên vs mục tiêu EEMUA-191** (P1≤5% · P2≤15% · P3≥80%). Runtime `alarmRationalization()` + 7 tag `ALM_RAT_*` + WS command `alarm-rationalization` + màn D2-alarm-rationalization. **TÁCH khỏi AlarmDef/engine (0 hồi quy)**; thời gian phản ứng nhất quán ưu tiên (P1 gấp hơn P3). **Phát hiện trung thực: subset an toàn nặng P1 (42%) → phân bố CHƯA đạt EEMUA** (cần rationalize toàn bộ ~650 alarm doc 08 §5). Thời gian phản ứng/căn cứ ưu tiên = [GIẢ ĐỊNH] (ma trận hậu quả × thời gian). Test: plugin rationalization (5) + app (3). | doc 08 |
| GĐ‑123 | Chiều sâu physics — **bao hơi shrink/swell 2 pha** (v1.59, A4): `DrumSwellModel` (ADDITIVE, đọc BLR_STEAM_FLOW_01 + BLR_DRUM_LEVEL_01). Hiệu ứng NON-MINIMUM-PHASE: tăng tải (hơi ↑) → áp tụt tức thời → bọt hơi NỞ → mức DÂNG (swell) dù khối lượng nước GIẢM; giảm tải → SHRINK. Swell ∝ tốc-độ-đổi-hơi (đạo hàm lọc) rồi phân rã (bọt ổn định). Sinh `DRM_*` (7): tốc độ đổi hơi · swell/shrink mm · mức biểu kiến vs mức thật ước lượng · đỉnh · lành mạnh. Malfunction `rapid-loadup`/`rapid-loaddown` (xung ép minh hoạ). **0 hồi quy: swell là thành phần CHẨN ĐOÁN tách ra, KHÔNG đổi BLR_DRUM_LEVEL_01 lõi** (test kiểm yên tĩnh biên độ→0). Màn D3-drum-swell + nav. Hệ số swell/tốc độ · hằng phân rã · biên xung = [GIẢ ĐỊNH]. Test: plugin drum-swell (4) + app (1). | doc 10 §6 |
| GĐ‑122 | Chiều sâu physics — **bình gia nhiệt PER-HEATER** (v1.59, A3): `HeaterDetailModel` (ADDITIVE, đọc GEN_MW_01). Phân giải từng bình HP1–HP3 + LP1–LP4 (mức nước ngưng · TTD · van drain). Van drain KẸT đóng → mức DÂNG (~92%) → ngập ống → TTD xấu (kém hồi nhiệt) → hụt nhiệt nước cấp. Khác feedwater-drains (GĐ-107, TỔNG HP/LP) ở granularity per-heater. Sinh `HTR_*` (26: 21 per-heater + 5 aggregate). Malfunction `hp1..3/lp1..4-drain-stuck`. **0 hồi quy: đọc GEN_MW, sinh HTR_* — không đổi FWH_*/tag nước cấp lõi** (ước lượng hụt nhiệt là chẩn đoán). Màn D3-heater-detail + nav, state (mức + bình kẹt) snapshot. TTD định mức 3°C · mức 50% · hệ số TTD-ngập = [GIẢ ĐỊNH]. Test: plugin heater-detail (5) + app (1). | doc 10 §6 |
| GĐ‑121 | Chiều sâu physics — **tối ưu cháy & hiệu suất lò** (v1.59, A2): `CombustionOptModel` (ADDITIVE, đọc BLR_FLUE_O2_01 + FG_STACK_TEMP_01 + PVM_MIN_FINENESS_01). Đường cong hiệu suất theo gió thừa: O₂ thấp → LOI/carbon-in-ash + tổn thất chưa cháy tăng; O₂ cao → dry gas loss tăng; có ĐIỂM TỐI ƯU ~3% O₂. Sinh `CMB_*` (9): O₂/gió thừa/O₂ tối ưu · tổn thất khói khô/LOI/chưa cháy · **hiệu suất LÒ ước lượng ~90%** · hụt so tối ưu · lành mạnh. Malfunction `o2-trim-high`/`o2-trim-low`. **LƯU Ý: hiệu suất LÒ HƠI (phía cháy ~90%) là CHẨN ĐOÁN riêng, TÁCH khỏi hiệu suất NET chu trình (M-06, 33%) — KHÔNG đụng heat rate lõi (0 hồi quy).** Màn D3-combustion-opt + nav. O₂ tối ưu · hệ số tổn thất · moisture/H₂ loss 6% = [GIẢ ĐỊNH]. Test: plugin combustion-opt (5) + app (1). | doc 10 §6 |
| GĐ‑120 | Chiều sâu physics — **AGC điều tần thứ cấp** (v1.59, A1): `AgcSecondaryModel` (ADDITIVE, đọc GOV_GRID_FREQ_01 + GEN_MW_01). SECONDARY control (chậm) khôi phục tần số về 50 Hz + đưa tie-line về lịch qua Area Control Error: ACE = ΔP_tie + B·Δf (tie-line bias B=240 MW/Hz). Tích phân điều tiết đưa ACE→0 trong vài phút. Nối tiếp governor/PFR (GĐ-118: primary chặn, secondary khôi phục). Sinh `AGC_*` (9): bias · lịch/dòng tie · ACE · điều tiết · setpoint · CPS · lành mạnh. Malfunction `agc-oos` · `tie-line-disturbance`. **0 hồi quy: điều tiết là tín hiệu AGC SẼ ra lệnh, không ghi GEN_MW lõi** (test kiểm ACE khôi phục). Màn D3-agc-secondary + nav, state (tie + tích phân) snapshot. Frequency bias · lịch tie · hệ số tích phân · biên ±120 MW = [GIẢ ĐỊNH]. Test: plugin agc-secondary (4) + app (1). | doc 10 §7 |
| GĐ‑117 | Chiều sâu physics — **máy nghiền than PER-MILL** (v1.58): `PulverizerMillsModel` (ADDITIVE, đăng ký SAU coalHandling). Phân giải TỪNG máy nghiền A–F (5 chạy + 1 dự phòng): tải % · **độ mịn % (lọt sàng 200 mesh)** · ΔP bát · trạng thái (0 dừng/1 chạy/2 trip). Khi 1 máy TRIP, tổng than phân bổ lại cho máy còn lại → tải leo (>100% quá tải) → **độ mịn TỤT** (grind thô → cháy kém). Khác coal-handling (v1.25, tải TRUNG BÌNH) ở phân giải per-mill + động học trip/redistribute. Tag `PVM_*` (25: 18 per-mill + 7 aggregate), màn D3-pulverizer-mills + nav. Malfunction `mill-a-trip`…`mill-e-trip` · `mill-classifier-wear` (mòn phân ly → độ mịn tụt toàn dàn). **0 hồi quy: đọc BLR_COAL_FLOW_01, sinh PVM_* — không đổi COAL_*/tag lõi** (test kiểm điểm vận hành 4 máy lành mạnh). Độ mịn định mức 76% ở 85% tải · hệ số độ mịn/tải · ΔP∝tải² · gió sơ cấp 1,8× than = [GIẢ ĐỊNH]. Test: plugin pulverizer-mills (5) + app (1). | doc 10 §6 |
| GĐ‑116 | Chiều sâu physics — **PSS bộ ổn định hệ thống điện** (v1.57): `PssStabilizerModel` (ADDITIVE, đăng ký SAU avrExcitation). PSS bơm tín hiệu phụ Vs vào tổng AVR để DẬP dao động điện-cơ local-mode ~1 Hz. Mô hình có DAO ĐỘNG NỘI (oscillator bậc 2 small-signal): nhiễu lưới kích cộng hưởng → biên độ ∝ 1/ζ. PSS bật → ζ=0,15 (dập tốt, biên ~5 MW); ngưng → ζ=0,03 (dai, biên ~17 MW); gain quá cao → ζ<0 (dao động TĂNG, minh hoạ mis-tune). Δω qua washout + lead → Vs (giới hạn ±5%). Tag `PSS_*` (8), màn D3-pss-stabilizer + nav + 3 ô trong D2-physics-depth (nay 21 ô), state (góc/tốc độ dao động + lọc) snapshot. Malfunction `grid-oscillation` · `pss-out-of-service` · `pss-gain-high`. **0 hồi quy: chỉ sinh PSS_*, dao động nội KHÔNG tác động lõi** (test kiểm GEN_MW không đổi; điểm vận hành biên độ→0 yên tĩnh). Tần số mode · ζ bật/tắt · gain Ks · giới hạn Vs · hệ số Δω = [GIẢ ĐỊNH]. Test: plugin pss-stabilizer (6) + app (3). | doc 10 §7 |
| GĐ‑115 | HMI — **màn tổng hợp chiều sâu physics D2** (v1.56): thêm màn khai báo `D2-physics-depth` (18 ô valueTile, 3 cột × 6 hàng) gom chỉ số 6 hệ chiều sâu đã dựng: AVR/kích từ (GĐ‑106) · bảo vệ ANSI (GĐ‑109) · drain cascade FWH (GĐ‑107) · hồi nhiệt/heat-rate (GĐ‑110) · bám bẩn & lọt khí (GĐ‑114) · CEMS Hg/CO (GĐ‑113). Chỉ THAM CHIẾU tag đã có + nút nav D2 (dưới D1-plant-overview); **KHÔNG sinh tag/physics mới, 0 hồi quy**. Ngưỡng cảnh báo hiển thị trên ô (vd Vterm < 19 kV · TTD > 6 °C · Hg > 5 µg/Nm³ · CO > 200 mg/Nm³) là **dải hiển thị suy từ ngưỡng model đã đăng ký** (GĐ‑106…114), không phải giả định số mới. Test: app physics-depth-screen (1, kiểm màn+nav có mặt & mọi tag SỐNG trong runtime) + probe headless (18 ô render sống, 0 đè, 0 lỗi JS). | doc 11 / doc 13 |
| GĐ‑71 | Tháp làm mát khép vòng CW (v1.24, doc 10 §6): `CoolingTowerModel` (ISimModel) chạy CẠNH …/condenser — đọc nhiệt thải/độ tăng nhiệt CW tươi → khép vòng nước tuần hoàn: `CT_WETBULB_01`·`CT_CW_SUPPLY_01` (= bầu ướt + approach) · `CT_APPROACH_01`·`CT_RANGE_01` · `CT_HEAT_REJECT_01` (= duty bình ngưng) · `CT_EVAP_LOSS_01` (bốc hơi = nhiệt thải/ẩn nhiệt) · `CT_MAKEUP_01` (bổ sung = bốc hơi + blowdown + drift). ADDITIVE (0 hồi quy). Neo Design Basis: tháp natural draft hyperbolic 165 m · CW 64.000 m³/h. Bầu ướt 27 °C (nhiệt đới VN) · approach 4 °C · ẩn nhiệt 2.400 kJ/kg · hệ số bổ sung 1,5 = [GIẢ ĐỊNH]. Bốc hơi ~1.335 t/h ở đầy tải. **KHÉP MÂU THUẪN v1.20:** bầu ướt nhiệt đới 27 °C → CW cấp ~31 °C → chân không THỰC sẽ > 5,4 kPa thiết kế (mốc ôn hoà CW ~19 °C, GĐ-67) — dữ kiện hiệu năng THẬT, không phải lỗi; tháp phản ánh điều kiện địa phương, 5,4 kPa là điểm thiết kế. Màn D3-cooling-tower (7 ô). Fan draft/fill/psychrometric đầy đủ + blowdown chemistry = pha sau | doc 10 |
| GĐ‑70 | Phía điện: máy phát → GSU → lưới + tự dùng → công suất NET (v1.23, doc 10 §7): `ElectricalModel` (ISimModel) chạy CẠNH …/turbine — đọc công suất gộp/phản kháng tươi → `ELEC_AUX_POWER_01` (tự dùng) · `ELEC_NET_MW_01` (net = gộp − tự dùng) · `ELEC_GRID_MW_01` (xuất lưới sau tổn thất GSU) · `ELEC_GEN_MVA_01`·`ELEC_PF_01`·`ELEC_GEN_CURRENT_01` (dòng stator 20 kV) · `ELEC_GSU_LOADING_01`·`ELEC_AUX_LOADING_01`. ADDITIVE (0 hồi quy — không đổi GEN_MW_01/GEN_MVAR_01). Neo Design Basis (Phụ lục A §3.3): gộp/tinh **600/558 MW (tự dùng ~7 %)** · lưới 500 kV/50 Hz · máy phát **667 MVA, 20 kV, cosφ 0,9** · GSU **20/500 kV, 720 MVA** · UAT **2×50 MVA**. Điểm thiết kế (600 MW, cosφ 0,9): net 558 ✓, MVA ~667 ✓, pf 0,9 ✓. Tự dùng nền 12 + biến thiên 30 MW · tổn thất GSU 0,4 % = [GIẢ ĐỊNH]. Lưu ý: sim chạy pf ~0,85 (Q_FACTOR turbine v1.12) → MVA cao hơn ở đầy tải; điểm vận hành ~448 MW tải GSU ~73 %. Màn D3-electrical (8 ô). Kích từ AVR/PSS · bảo vệ ANSI (87/40/46…) · switchyard = pha sau | doc 10 §7 |
| GĐ‑69 | Phát thải CEMS (bụi/SO₂/NOₓ/CO₂ sau ESP+FGD) (v1.22, doc 10 §6): `EmissionsModel` (ISimModel) chạy CẠNH …/fluegas — đọc than/khói/gió thừa tươi → tính tải phát thải từ THÀNH PHẦN THAN (tro/S/C) rồi khử qua ESP/FGD, quy nồng độ mg/Nm³ (CEMS): `EMI_DUST_STACK_01` (< 30 mg/Nm³ đạt mốc DB) · `EMI_SO2_STACK_01` (sau FGD) · `EMI_NOX_STACK_01` · `EMI_CO2_RATE_01` (~640 t/h) · `EMI_FG_VOLUME_01` · `EMI_ESP_EFF_01`·`EMI_FGD_EFF_01`. ADDITIVE (0 hồi quy). Neo Design Basis (Phụ lục A): tro 15 % · S 0,6 % · ESP 2×4 trường bụi ra < 30 mg/Nm³ · sơ đồ khói có FGD. Hàm lượng C 62 % · tỷ lệ tro bay 0,85 · độ khử ESP 0,9977 (hiệu chỉnh đạt < 30) · FGD 0,95 · ρ khói 1,3 · NOₓ nền 320 · thể tích khói riêng = [GIẢ ĐỊNH]. Bảo toàn khối lượng: CO₂ ∝ than (carbon). Màn D3-emissions-cems (7 ô). Số kiểm định/CEMS thật + Hg/CO + FGD gypsum + ESP theo trường = pha sau | doc 10 |
| GĐ‑68 | Đường khói + gió cháy + hiệu suất lò (v1.21, doc 10 §6): `FlueGasAirModel` (ISimModel) chạy CẠNH boiler/…/feedwater — đọc than/O₂/công suất/HR chu trình tươi → khép kín phía KHÓI-GIÓ: `BLR_EFF_01` (hiệu suất lò = nhiệt vào hơi / nhiệt nhiên liệu ~88 %) · `FG_EXCESS_AIR_01`·`FG_LAMBDA_01` (từ O₂) · `FG_FLOW_01` · `FG_AH_GAS_IN_TEMP_01`·`FG_STACK_TEMP_01`·`AH_AIR_OUT_TEMP_01` (air heater) · `FG_DRYGAS_LOSS_01`. ADDITIVE (0 hồi quy, khớp hằng số boiler LHV 21.500 · AF_STOICH 10 · COAL_MAX 300). Neo Design Basis: than bituminous LHV 21.500 kJ/kg · tro 15 % · ẩm 10 % · O₂ 3,2 % · air heater 2×Ljungström · ống khói 210 m. Nhiệt air heater/ống khói (350/130 °C) · cp khói 1,05 · độ hữu hiệu AH 0,84 · T môi trường 30 = [GIẢ ĐỊNH]. **Đối chiếu heat rate:** heat rate ĐƠN VỊ = HR chu trình / hiệu suất lò ≈ 8.940/0,88 ≈ **10.150 kJ/kWh (gross)**; Design Basis **9.200 (net, 39 %)** hàm ý η chu trình cao hơn hằng số enthalpy [GIẢ ĐỊNH] hiện tại (GĐ-66) — chênh ~10 % là hạng mục **HIỆU CHỈNH bằng heat balance thật**, không phải lỗi mô hình (nêu rõ). Màn D3-fluegas-air (8 ô). CEMS/ESP/FGD/tháp làm mát chi tiết = pha sau | doc 10 |
| GĐ‑67 | Bình ngưng + nước tuần hoàn CW (v1.20, doc 10 §6): `CondenserCWModel` (ISimModel) chạy CẠNH boiler/turbine/reheat/feedwater — CÂN BẰNG NĂNG LƯỢNG: nhiệt thải = nhiệt cấp (HR chu trình·gross) − công suất → sinh tag phía CW (`COND_DUTY_01`·`COND_SAT_TEMP_01`·`COND_TTD_01`·`COND_CW_FLOW_01`·`COND_CW_IN/OUT_TEMP_01`·`COND_CW_RISE_01`). ADDITIVE — CHỈ ĐỌC `TRB_COND_VACUUM_01` (boiler sở hữu), KHÔNG ghi đè (0 hồi quy; malfunction loss-of-vacuum vẫn tác động qua boiler). Neo Design Basis (Phụ lục A): CW **64.000 m³/h** · chân không 5,4 kPa → sat ~34 °C · tháp làm mát natural draft. ΔT_cw = Q_thải/(ṁ_cw·cp) ~12 °C; CW ra = sat − TTD; CW vào = ra − ΔT. **Nhiệt thải ~890 MWth > công suất 600** (η<50% → phần lớn nhiệt bị thải — hiện rõ qua cân bằng). TTD 2,8 °C · cp 4,18 · tương quan sat-temp · τ CW = [GIẢ ĐỊNH]. **Lưu ý thật:** 5,4 kPa ⇒ CW vào ~19 °C (ôn hoà); vận hành nhiệt đới (CW vào cao hơn) đẩy back-pressure lên — cảnh báo hiệu năng thật, không phải lỗi mô hình. Màn D3-condenser-cw (7 ô). Fouling/air-ingress động + tháp làm mát chi tiết = pha sau | doc 10 |
| GĐ‑66 | Gia nhiệt HỒI NHIỆT (regenerative) + heat rate chu trình (v1.19, doc 10 §6): `FeedwaterTrainModel` (ISimModel) chạy CẠNH boiler/turbine/reheat — đọc hơi/MW/chân không/nhiệt reheater tươi → sinh tag đoàn gia nhiệt nước cấp (`FW_CONDENSATE_TEMP_01`·`FW_DEAERATOR_TEMP_01`·`FW_ECON_INLET_TEMP_01`·`FW_FLOW_01`·`FW_REGEN_DUTY_01`) + heat rate chu trình (`PLANT_CYCLE_HR_01`). ADDITIVE (0 hồi quy). Neo Design Basis (Phụ lục A): econ inlet **283 °C** · deaerator 0,9 MPa/**178 °C** · condensate ~34 °C (bão hoà 5,4 kPa) · 3 HP + 4 LP heater · đường: condensate→4 LP→deaerator→BFP→3 HP→econ. Nhiệt các mốc trượt theo tải (regFrac = 0,5 + 0,5·loadFrac); regen duty = ṁ·cp·ΔT; heat rate = (ṁ·Δh_boiler + reheat)·3600/gross — nước cấp CÀNG NÓNG → nhiệt cấp càng ÍT → heat rate tốt hơn (lợi ích hồi nhiệt hiện rõ qua chính con số). **Heat rate CHU TRÌNH turbine (~8.900) KHÁC heat rate ĐƠN VỊ 9.200 kJ/kWh** (đơn vị gồm hiệu suất lò ~88% + tự dùng) — nêu rõ, không nhầm lẫn. cp 4,4 kJ/kgK · Δh_boiler 2.145 kJ/kg · đường regFrac · K_vac = [GIẢ ĐỊNH]. Màn D3-feedwater-heatrate (6 ô). Từng heater riêng + drain cascade + bảng hơi enthalpy = pha sau | doc 10 |
| GĐ‑74 | Chiều sâu SCADA đoàn nước cấp — nhiệt đầu ra TỪNG bình gia nhiệt (v1.27, doc 10 §6): `FeedwaterTrainModel` phơi thêm `FW_LPH1..4_TEMP_01` (4 LP heater) + `FW_HPH1..3_TEMP_01` (3 HP heater) — nội suy bậc ĐỀU giữa các nút vật lý ĐÃ CÓ (condensate→deaerator cho LP; deaerator→econ cho HP), CÙNG physics hồi nhiệt GĐ‑66, chỉ tính ở nhiều nút hơn (đơn điệu tăng, tất định, KHÔNG bịa — không thêm trạng thái, snapshot/restore không đổi). ADDITIVE (0 hồi quy — 6 tag cũ giữ nguyên giá trị). Cho phép màn D3-feedwater-heatrate vẽ **bản vẽ bố trí đầy đủ 11 khối** (condensate→4 LP→khử khí→BFP→3 HP→economizer), mỗi bình có nhiệt sống riêng. Chia enthalpy ĐỀU/bình = [GIẢ ĐỊNH] (thực TTD/DCA từng bình khác nhau) — thay bằng heat balance nhà chế tạo khi có. Drain cascade + hơi trích enthalpy từng tầng = pha sau | doc 10 |
| GĐ‑65 | Chu trình TÁI NHIỆT + turbine nhiều tầng (v1.18, doc 10 §6): `ReheatCycleModel` (ISimModel) chạy CẠNH boiler/turbine — đọc hơi chính/áp/nhiệt/MW tươi mỗi bước → sinh THÊM tag đường reheat (`TRB_CRH_PRESS/TEMP_01`, `TRB_HRH_PRESS/TEMP_01`) + tách công suất `TRB_HP/IP/LP_MW_01` + nhiệt lượng `TRB_REHEAT_DUTY_01`. ADDITIVE — các tầng CỘNG lại = `GEN_MW_01` (0 hồi quy, không đổi trục). Neo Design Basis (Phụ lục A): hơi chính 17,5 MPa/541 °C · cold reheat 4,2/330 · hot reheat 3,8/541 · BMCR 2.008 t/h · tandem-compound HP+IP+2×LP. Áp CRH/HRH TRƯỢT theo áp hơi chính (PR_HP = 4,2/17,5); nhiệt CRH bám (hơi − ΔT_HP = 211), HRH điều về ~541 (droop khi non tải); duty = ṁ·Δh_reheat. Phân bổ tầng F_HP/IP/LP = 0,28/0,30/0,42 · Δh_reheat 526 kJ/kg · droop 60 °C · idle 200 °C = [GIẢ ĐỊNH] (suy từ enthalpy drop áp/nhiệt DB; số heat-balance thật lấy khi có). Hiện trên màn D3-turbine-generator (8 ô). Extraction + gia nhiệt hồi nhiệt (3 HP + 4 LP heater) & bảng hơi đầy đủ = pha sau | doc 10 |
| GĐ‑64 | System Diagnostic (v1.17, màn hình hệ thống S — ISA-101): `systemDiagnostics()` READ-ONLY tự soi trạng thái runtime từ NGUỒN THẬT (không bịa): sim (bước·frozen·đồng hồ), tag (số ghi·Good·khác + catalog §10), control loop (tổng·AUTO·MAN·CAS qua `getMode`), historian (điểm qua `size()` + range), alarm (active + P1..P4), journal (tổng·lastSeq). Server lệnh `diag` (read-only, cần login); HMI nút "🩺 Chẩn đoán hệ thống" + modal. Là phương thức APP-LEVEL (sức khoẻ NỀN TẢNG, không phải dữ liệu plant → không cần plugin khai báo, không phá tính generic). Với v1.16/1.17, bộ màn hình hệ thống ISA-101 (S) đã CÓ MẶT ĐỦ: Alarm Summary/History · Trend · Historian-Replay · Event Log · Report · KPI · Maintenance · Engineering (Screen Builder) · User Mgmt (RBAC + audit) · System Diagnostic · AI Advisor. Watchdog/latency/self-test/health-check bên thứ ba = production | ISA-101 |
| GĐ‑63 | Event Log / SOE (v1.16, màn hình hệ thống S — doc 00 §màn hình hệ thống; audit doc 15/18): `EventJournal` GENERIC (ring buffer, seq đơn điệu) ghi lại SỰ KIỆN đã xảy ra — 6 danh mục trung tính (alarm·trip·command·sequence·security·system, KHÔNG tên plugin → giữ tính generic). READ-ONLY với process: chỉ quan sát & ghi, KHÔNG đụng tag/setpoint. Runtime tự bắt: alarm raised/cleared (dedupe theo alarmId), trip C&E chốt (dedupe theo effect), lệnh (đặt tải·ACK·đổi mode·WO·reset C&E·khởi động SFC), hệ thống (tiêm/gỡ malfunction·freeze OTS), chuỗi SFC kết thúc; server đẩy sự kiện bảo mật (login OK/thất bại). `eventLog(query)`/`eventSummary()`; server lệnh `journal` (read-only, cần login); HMI nút "📓 Nhật ký sự kiện" + modal (tổng hợp theo danh mục/mức + bảng giờ·loại·sự kiện·người). Denial lệnh vẫn ở audit RBAC riêng (audit-query, doc 18). Dấu thời gian = đồng hồ sim (không Date.now trong vòng). Lọc nâng cao + xuất + lưu bền = v2 | doc 15/18 |
| GĐ‑62 | Report/KPI engine (v1.15, doc 21): `ReportEngine` GENERIC ráp báo cáo từ section KHAI BÁO (`IReportSection.render(ctx)` → khối table/text/trend), không hardcode nội dung. Đọc aggregate (avg/min/max) qua `IReportContext` (CHỈ ở SDK — plugin không phụ thuộc engine; `range` gắn trong ctx). `thermalReportSections` (3 mục: operating-summary = bảng 6 tag chính avg/min/max · narrative = text từ avg · trend = MW+lưu lượng hơi). Runtime `generateReport(hours)` bơm ctx đọc từ Historian (`query` agg theo range lịch sử); server lệnh `report` (read-only, cần login); HMI nút "📋 Báo cáo ca" + modal render table/text/trend. READ-ONLY tuyệt đối (chỉ đọc lịch sử, không ghi tag). Designer báo cáo kéo-thả + narrative sinh bởi AI + xuất PDF/Excel = v2 | doc 21 |
| GĐ‑61 | Breadth "sống" (v1.14, doc 05-05): `RegistrySimModel` sinh giá trị PLACEHOLDER cho toàn §10 catalog (3.6k tag chưa có physics riêng) → cả nhà máy "thở", mọi màn hình (kể cả 87 màn catalog + màn hình builder) có dữ liệu. Analog ~40% dải + nhiễu LCG có seed (KHÔNG Math.random); bool/int = 0 → không kích alarm breadth (ở biên dải). Chạy stride (mỗi 10 bước) để nhẹ ingest. Opt-in `breadthLive` (mặc định TẮT → test không đụng); server BẬT. Tag sim thật (boiler/turbine) tách biệt, không đụng. Là DỮ LIỆU OTS/demo, KHÔNG physics — [GIẢ ĐỊNH]. Runtime expose `catalogScreens()`; server phục vụ 87 màn qua /screen | doc 05-05 |
| GĐ‑60 | Chốt FEATURE-COMPLETE v1.13 (doc 24): toàn bộ tính năng roadmap v1→v3 đã hiện thực ở mức code — kernel · 12 engine L2 · §10 breadth (3.610 tag/662 alarm/87 màn/29 loop/8 SFC/2 C&E/kịch bản) · trip đổi physics · SFC live · OTS · Historian TimescaleDB + benchmark ≥50k · MQTT Sparkplug · re-sim (+UI banner cam) · AI advisor · Screen Builder L5 (kéo-thả) · Predictive maintenance · Turbine/Generator sim. Thêm màn hình D3-turbine-generator (hiện tag v1.12). **Còn lại = độ chín production** (turbine nhiều tầng/reheat/extraction, sim thật 34 hệ BoP, cắm broker/DB/OPC-UA thật, kiểm định bên thứ ba) — cần hạ tầng/nhân lực/thời gian ngoài phạm vi, ~2–2,5 năm (GĐ-03). build 7/7 · 177 test | doc 24 |
| GĐ‑59 | TurbineGeneratorModel — chiều sâu vật lý (v1.12, doc 10 §6/§7): ISimModel chạy CẠNH boiler (đăng ký sau → đọc hơi/áp/chân không/MW tươi), sinh THÊM tag TRB_STODOLA_FLOW (ellipse Stodola √(pin²−pout²)) · TRB_SPEED_01 (swing quanh 3000 rpm) · GEN_FREQ_01 (speed/60) · GEN_MVAR_01 (0,62·MW ≈ pf 0,85) · GEN_STATOR_TEMP_01 (τ nhiệt). ADDITIVE — không đổi GEN_MW_01 boiler (0 hồi quy). 3000 rpm/50 Hz & vacuum 5,4 kPa neo Design Basis; K_STODOLA/K_IMB/Q_FACTOR/stator = [GIẢ ĐỊNH]. Turbine đầy đủ (nhiều tầng, reheat, extraction) = pha sau | doc 10 |
| GĐ‑58 | Screen Builder UI kéo-thả (v1.11, doc 05-02 L5): HMI modal — chọn tag (picker từ danh sách tag server gửi khi connect), thêm ô, sửa nhãn/đơn vị/kiểu (số/bar), **kéo-thả sắp xếp thứ tự** (HTML5 draggable), số cột → gửi `build-screen` (đã có, action engineer) → mở màn hình vừa tạo. Người không code tạo màn hình hoàn toàn bằng thao tác chuột. Server thêm message `tags` (nguồn picker = recordedTags) | doc 05-02 |
| GĐ‑57 | Predictive Maintenance v1 rule-based (v1.10, doc 05-18/20): `PredictiveMaintenance` READ-ONLY — 3 loại luật: ngưỡng (value≥warn/limit), xu hướng (rate → dự báo giờ tới limit), giờ chạy (tới hạn PM). Trạng thái nhẹ (mẫu tag trước) để tính rate. `thermalPredictiveRules` (5 luật: vacuum trend+threshold, SH temp trend, PM UNIT1/MILL). Runtime `evaluatePredictive()`/`predictiveAdvisories()`; server gộp vào maint broadcast; HMI hiện "🔮" trong maint strip. Ở điểm vận hành không cảnh báo; mất chân không → cảnh báo. Ngưỡng/mốc PM = [GIẢ ĐỊNH] (GĐ-38/40). ML thật = v3 | doc 05-18 |
| GĐ‑56 | Screen Builder L5 (v1.9, doc 05-02): `buildScreen(spec)` — người KHÔNG code chỉ liệt kê tag + nhãn (+alarm/bar tuỳ chọn); engine tự dàn lưới + sinh binding → ScreenDef hợp lệ, kernel render. Server lệnh `build-screen` (action engineer) dựng + đăng ký vào screensById/registry → phục vụ ngay qua /screen/{id}; HMI nút "🧩 Tạo màn hình demo" → mở màn hình vừa dựng. UI kéo-thả đầy đủ = pha sau (bản này bỏ gõ toạ độ/binding tay) | doc 05-02 |
| GĐ‑55 | UI Re-simulation banner cam (v1.8, doc 05-05 §4): server lệnh `resim` (read-only) chạy `reSimulate` nhánh what-if (malfunction hoặc override tải) → trả quỹ đạo; HMI 2 nút "What-if", overlay **banner cam** + biểu đồ canvas MW theo bước (500 bước, độc lập sim live). Không chặn khi replay (read-only, không đụng live) | doc 05-05 |
| GĐ‑54 | AI Advisor v1 rule-based (v1.7, doc 20): `AiAdvisor` READ-ONLY tuyệt đối (chỉ đọc alarm/tag/tri thức; KHÔNG ghi tag/setpoint/ACK). Giải thích alarm = hậu quả + nguyên nhân (theo điều kiện+ngưỡng) + khắc phục + LEO THANG (nếu tag là cause của ma trận C&E) + TRÍCH DẪN nguồn (SOP/narrative/cause-effect + tag + thời gian). Chống bịa: chỉ trích cái có thật. Tri thức plugin (`thermalKnowledge`: 4 doc SOP/C&E/narrative). Server lệnh `advise` (read-only, không chặn replay); HMI nút "💡 Giải thích" + panel. RAG/LLM thật = v2 | doc 20 |
| GĐ‑53 | Re-simulation what-if (v1.6, doc 05-05 §4): `reSimulate()` chạy NHÁNH mô phỏng ĐỘC LẬP — SimulationHost + ControlLoopEngine + kho tag riêng, khôi phục từ snapshot live, chạy kịch bản khác (override/malfunction) → quỹ đạo tag. Sim/tag LIVE KHÔNG bị đụng (kho tag riêng; chỉ đọc snapshot live). Loop nhánh khởi tạo AUTO (transient nhỏ; so sánh tương đối base↔nhánh để loại nhiễu). UI banner cam + biểu đồ quỹ đạo = pha sau (hiện là API + test) | doc 05-05 |
| GĐ‑52 | Driver MQTT Sparkplug B (v1.5, doc 16): `SparkplugDriver` (IProtocolDriver) vai trò HOST — nhận NBIRTH/NDATA, xử lý ĐÚNG alias (BIRTH lập name↔alias, DATA chỉ gửi alias), lọc theo interest → `ctx.publish` vào kernel; `write`→NCMD. `MqttTransport` + `SpCodec` INJECT → lõi không phụ thuộc mqtt.js/protobuf, kiểm được không cần broker. Serialize mặc định JSON; deploy dùng protobuf Eclipse Tahu. metricMap (name↔tag id) do cấu hình cấp | doc 16 |
| GĐ‑51 | Historian TimescaleDB + benchmark (v1.4, doc 19/24): `TimescaleHistorian` cùng chữ ký write/query — đạt mốc ghi **≥ 50.000 điểm/s** bằng write() ĐỆM đồng bộ + flush() BATCH (1 INSERT nhiều dòng). SQL qua `SqlExecutor` INJECT (deploy: node-postgres; test: executor giả) → lõi `@idtp/engines` KHÔNG phụ thuộc `pg`, kiểm được không cần DB. Benchmark loadgen (100k điểm tất định): Memory ~772k điểm/s, Timescale-đệm ~4,37M điểm/s (≫ 50k, biên rộng; throughput xuống DB thật đo tại triển khai). `docker-compose.yml` (Timescale 2.17-pg16) + `docs/benchmark.md`. Ngưỡng bucket/chu kỳ flush = [GIẢ ĐỊNH] | doc 19/24 |
| GĐ‑50 | OTS control UI (v1.3): server + HMI điều khiển OTS trực quan — nút chạy SFC "live" + panel Cause&Effect (trạng thái trip + Reset). RBAC: `seq-live-start` & `ce-reset` = action `engineer` (Engineer+, single-step) — cùng lớp OTS freeze; broadcast `seq-live`/`ce` report-by-exception, gửi `seq-list`/`ce` khi kết nối. (Chạy SFC là chức năng OTS/huấn luyện → gate Engineer, không thêm PermissionAction mới) | doc 05-07 |
| GĐ‑49 | SFC nối vào sim (v1.2, doc 09/10): sim đọc tag lệnh chuỗi → `BLR_FW_FILL_CMD` bơm điền lò (FILL_TPH=300 t/h [GIẢ ĐỊNH]) nâng mức; `BLR_MILL_A_STOP_CMD` bớt 1 mill công suất (mill-a-start xoá cờ). Runtime thêm đường chạy SFC "live" (tick theo nhịp sim, đồng hồ THẬT) để chuỗi tác động physics qua thời gian — khác runSequenceToCompletion (đồng hồ ảo, không bước sim). Ở điểm vận hành các tác động này nằm trong dải bù của CCS (đúng thực tế); kiểm chứng vật lý ở mức sim (không CCS) + kiểm live execution ở runtime. Cascade turbine-trip→áp tăng→MFT: driver (áp tăng khi draw=0) kiểm ở sim; C&E hoàn tất khi áp>19,3 | doc 09/10 |
| GĐ‑48 | Trip đổi PHYSICS (v1.1, doc 10 §9): BoilerIslandModel đọc flag trip do C&E chốt → **MFT** (BLR_MFT_TRIP/MILLS_TRIP/FUEL_VALVES_CLOSE/PA_FANS_TRIP) cắt than về 0 → hơi/áp sập; **turbine trip** (TRB_TRIP/MSV_CLOSE) → đóng draw, MW=0, áp tăng. MW = min(lệnh, hơi sinh)×hệ số chân không → MFT làm MW tụt theo hơi. resetCauseEffect xoá luôn flag → phục hồi (OTS: trip→reset→restart). Vận hành thường không đổi (flag mặc định 0). Hệ quả: C&E tự tác động trên CCS live — leak mạnh (≥ công suất bù ~600 t/h) đẩy drum tới −250 → MFT (test alarm bắt LOW ngay lúc nổi thay vì chờ trạng thái cuối) | doc 10 §9 |
| GĐ‑47 | CauseEffectEngine + thermalCauseEffect (doc 09 §4, NFPA 85): **2 ma trận** boiler-MFT (6 nguyên nhân × 4 hệ quả) + turbine-trip (3×2) — §10 C&E ✓. Engine generic latch trip, reset chỉ khi hết nguyên nhân. Đánh giá mỗi bước trên CCS thật; test kiểm inject loss-of-vacuum → turbine trip chốt. Hệ quả ghi tag flag `*_TRIP` (actuation nối vào sim = sâu hơn, để sau); ngưỡng ngoài Design Basis (vacuum>20 kPa, ms>19,3) = [GIẢ ĐỊNH] | doc 09 §4 |
| GĐ‑46 | ScenarioRunner + thermalScenarios (doc 22, nghiệm thu §10): kịch bản **cold-start→light-off→fill→mill start→turbine roll→sync→ramp 550→mill trip→runback→MFT/coast-down** (10 pha) chạy end-to-end trên CCS thật — test kiểm quỹ đạo MW (ramp lên, coast-down xuống). Engine generic ráp SFC + malfunction + tải qua ScenarioHost. Pha khởi động chạy SFC mang tính ĐIỀU PHỐI (sim warm-start ở điểm vận hành; physics cold-start đầy đủ từ nguội = pha sim sâu hơn, để sau). MFT mô phỏng bằng cắt tải (=0); số bước settle = [GIẢ ĐỊNH] | doc 22 |
| GĐ‑45 | SequenceEngine + thermalSequences (doc 09, ISA-88/NFPA 85): **8 chuỗi SFC khai báo** (mill start/stop · boiler purge · light-off · feedwater fill · turbine roll · gen sync · unit runback) — §10 "8 sequence" ✓. Engine generic thông dịch SequenceDef (permissive-gate · actions ghi có audit · transition tag + holdMs · timeout→failed), không hardcode logic bước. Thời gian giữ/ngưỡng bước = [GIẢ ĐỊNH] (mô phỏng, tinh chỉnh khi vận hành); tag lệnh `*_CMD` là điểm breadth. runSequenceToCompletion chạy đồng hồ ảo (không đụng đồng hồ/CCS sim) | doc 09 |
| GĐ‑44 | LoopGenerator (doc 09): định nghĩa GENERIC "loop = instance có cả `_PV` + `_OP`" → sinh **29 ControlLoopDef** breadth (≥ 25 §10 ✓), chưa gồm 7 loop CCS live đã tinh chỉnh của Boiler Island. Là DANH MỤC: mode khởi tạo MAN, tuning mặc định kp1/ki0,1/kd0 + SP=trung điểm dải PV ([GIẢ ĐỊNH], tinh chỉnh khi đưa vào vận hành). Thêm 4 nhóm control_loop BoP (FGD·than·khí nén·HVAC) → registry lên **3.610 tag / 662 alarm / 87 màn hình** | doc 09 |
| GĐ‑43 | ScreenGenerator (doc 12): sinh **87 màn hình** khai báo từ registry (47 D3/nhóm thiết bị · 18 D2/cell tổng quan · 18 D4/cell chẩn đoán), đạt §10 ≥ 70. Là DANH MỤC: không ép vào nav live (tránh ô rỗng vì đa số tag breadth chưa có dữ liệu sim); nav live vẫn dùng 3 màn hình boiler thủ công. Ô đại diện = float đầu (D3/D2) hoặc tag có alarm (D4); trần 12 ô/màn hình | doc 12 |

## 2. Vấn đề mở
> ✔ M‑05 (bài test generic): thêm `water-treatment-demo` chỉ chạm `plugins/water-treatment-demo/*` + `pnpm-lock.yaml` — **0 dòng sửa `packages/*` & `apps/*`** (kiểm bằng `git status`). Luận điểm "mọi nhà máy chỉ là plugin" đứng vững.
> ✔ Doc 06 (process-analysis-thermal): **45 hệ thống × 13 mục** + 4 mục chuyên sâu (MFT/turbine-trip C&E · purge NFPA 85 · generator protection ANSI) — đạt §10 ≥ 40 hệ. Tag/alarm/màn hình/model ánh xạ hiện thực đã dựng; hệ BoP "(breadth)" có tag danh mục sống, physics riêng = pha sau.
> ✔ Doc 05 (engine-specs): **21 engine L1/L2 × 10 mục §8** (Mục đích/ranh giới · Interface TS · Mô hình dữ liệu · Luồng Mermaid · Cấu hình · Phi chức năng · Chế độ lỗi · Plugin mở rộng · Kiểm thử · Quyết định thiết kế). Chữ ký interface ánh xạ mã đã hiện thực; Trend/Animation (L4 client) không lập chương riêng. → **Bộ tài liệu 00–25 nay đã đủ file** (nợ doc 05/06 đã trả).
> ✔ CI (doc 23): `.github/workflows/ci.yml` — build + test toàn workspace trên mỗi push/PR (matrix Node 20/22, pnpm 9.7.0, frozen lockfile). Cổng kiểm tra hồi quy tự động (232 test).
> ✔ Seed §10 (doc 07 §5): `SeedGenerator` (engine generic) + `thermalSeedSpec` (dữ liệu plugin) → **3.522 tag / 651 alarm**, đạt ngưỡng nghiệm thu §10 (≥ 3.000 / ≥ 600). Bề rộng = thêm DỮ LIỆU khai báo, engine/kernel không đổi (GĐ‑42).
> ✔ **Test coverage ≥ 70% (GĐ‑30):** thêm `@vitest/coverage-v8` + unit test plugin cho 5 sim model BoP chưa phủ (`ash-handling` · `compressed-air` · `fuel-oil` · `bypass-airremoval` · `calibration`) + test registry khai báo (screens/loops/nav/faceplate/interlock/kpi — bất biến + tham chiếu chéo) + 3 module kernel (`audit` · `data-contract` · `time-service`). **Coverage stmts theo package (mã runtime): engines 89,7 % · plugin 97,1 % · app 82,9 % · kernel 93,9 % — đều ≥ 70 %**; `@idtp/sdk` là type-only (0 câu lệnh runtime → ngoài phạm vi coverage). Bộ test **318 → 352 xanh**, 0 hồi quy.
| # | Vấn đề | Cần |
|---|---|---|
| M‑01 | Hiệu chỉnh GĐ‑02 (person‑month) bằng velocity thực Pha A | đo khi code |
| M‑02 | Đối chiếu toàn bộ KKS với VGB‑B 106 | tài liệu chuẩn |
| M‑03 | Xác nhận có FGD hay không (doc 06‑39) | chủ đầu tư |
| M‑04 | Tuning PID + τ/θ trên sim thực | pha code |
| M‑05 | Mốc chuyển lát cắt → v1‑complete | lập lịch doc 24 |
| M‑06 | **Hiệu suất net mô hình ~33 % vs thiết kế 39 %** (heat rate ~10.900 vs 9.200). **QUYẾT ĐỊNH v1 (C1): GIỮ 33 % TRUNG THỰC** — không ép số cho đẹp; với digital twin, số suy ra nhất quán nội bộ > số cosmetic. Đã THỬ hiệu chỉnh 1 hằng số `DH_EVAP` 2758→2340 (GĐ‑32): **phá vỡ điểm vận hành CCS** (áp sập ~8 MPa · cân bằng NL vỡ · ~10 test sub‑model đỏ · trip giả) → **hoàn tác**. Kết luận: 33 % gắn chặt operating point + seed/gain 7 vòng (GĐ‑32) + ngưỡng mọi sub‑model ⇒ chỉ sửa ĐÚNG bằng **tái dẫn xuất TOÀN BỘ điểm vận hành**, KHÔNG phải tweak hằng số. **Thủ tục retune shovel-ready: `docs/27-calibration-playbook.md`** (đầu vào bắt buộc · 6 bước · tiêu chí nghiệm thu) | heat balance nhà chế tạo (bắt buộc) + re‑tune seed/gain + cập nhật ~10 test |

## 3. Chống bịa
Mọi số không có trong Design Basis đã gắn `[GIẢ ĐỊNH]` và đăng ký ở đây. Không trích số điều khoản
tiêu chuẩn khi không chắc — mô tả nguyên tắc.
