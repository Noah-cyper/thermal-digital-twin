{
  "name": "idtp",
  "version": "0.1.0",
  "private": true,
  "description": "Industrial Digital Twin Platform (kernel + plugin)",
  "packageManager": "pnpm@9.7.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "2.1.9",
    "turbo": "^2.0.9",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
